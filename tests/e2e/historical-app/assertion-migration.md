# Historical browser assertion migration ledger

This ledger records test-owner and semantic migrations, not current Program UX completion. No skipped test or reduced scenario set is introduced.

| Change | Source evidence | Preserved / strengthened assertion | Status |
| --- | --- | --- | --- |
| Dedicated historical Surface owner | `PersonalWorkspacePocRoute.tsx:122` passes the actual Surface as `legacy`; `ProgramApp.tsx:37,480` no longer mounts it | All seven legacy runtime specs keep original URLs, real boot/storage and production components; only test-build Program import resolves to legacy node | Harness built; complete baseline 38/58 passed, subsequent approved migrations under verification |
| React `작성 화면` → `작성 단계`; draft lane kept separate | `docs/specs/2026-09-05-flowme-integrated-poc-gap-implementation-v1/k3a-design.md:208,212`; `PersonalWorkspacePocAuthoringSurface.tsx:3726-3727,3740-3742` | Exactly two input/result editing steps remain required. React additionally requires the outside `내 초안` action. Standalone retains `작성 화면` with exactly input/result/drafts; only input/result are editing steps | Both parity scenarios passed in the second full run |
| Default My fallback accepts normal `sort=next` canonicalization | `docs/specs/2026-08-11-r3b-approved-plan-execution-boundaries/spec.md:95`; operating `approved-plan-execution-ux.spec.ts:461`; actual fallback observation | Only `/my` or `/my?sort=next`, visible operating heading, both PoC owners absent, exact synthetic operating bytes, and zero storage calls | Updated invalid-gate step; needs targeted rerun |
| Four-group property chooser instead of 16 simultaneous rows | `k3a-design.md:13,57,66-68,193`; `personal-workspace-poc-authoring-chooser.ts:16-21` | Initial four groups / no property rows. Visit all groups, exact ordered canonical keys, all 16 editable, no duplicate/missing key. Existing time/zone, subcheck, native Undo, near-miss, storage and five-viewport checks remain | 16-key/native interaction scenario passed; viewport cancel/reopen path under verification |
| Screenshot destination | Existing runtime tests wrote generated captures into tracked historical report assets | Only output paths move to `output/playwright/historical-current/<original-stage>/`; image capture and assertions retained | Nine destinations in six specs changed; parity spec had no screenshot writer |

## Not automatically migrated

The D1-010 trash entry now uses the existing `selectView` visible-label helper, retaining the exact `휴지통 1` count and adding the resulting trash-surface assertion. The actual mobile button has `aria-label="개인공간 모바일 보기: 휴지통 1"` (`PersonalWorkspacePocSurface.tsx:5848-5850`); its prior bare accessible-name lookup never reached deletion/restore checks. Root explicitly approved this navigation-only migration. All deletion, Undo, restore, reload and operating-byte assertions remain.

- Immediate template insertion versus preview plus explicit apply remains unresolved for original D2-049 equivalence. Root approved testing the later K3A:41 / KA-B08/B09 path as current historical-runtime regression, not closing alpha M4. Selection/preview must leave raw text and storage unchanged; each exact scaffold is applied once with native Undo/Redo. The structure catalog's exact example bytes replace the earlier example fixture only for the current preview. Original D2-049 equivalence is still unconfirmed.
- Added `personal-section-title` has explicit P2C / K3B requirements (`2026-09-03-flowme-integrated-poc-personal-editing-closure-v1/spec.md:31`; `k3b-plan-section-order-design.md:62`). Root approved adding exactly this field to the expected schema list, preserving order and rejecting unknown extra fields.
- Harness-prefetch 404s for `/`, `/flows`, `/calendar` were caused by missing test-app route closure, not product defects. Real root pages were re-exported, the harness rebuilt, and the full run restarted. The incomplete pre-fix run is not the final baseline.
- Timeouts during concurrent browser activity require trace review; they are not automatically UX defects or approved test migrations.

## Execution evidence (2026-09-20)

- Complete baseline after test-harness route closure: 58 executed, 38 passed, 20 failed, 0 skipped, 26.7 minutes. JSON: `tests/e2e/output/playwright/historical-surface-summary.json` (initial reporter resolved relative to config directory); screenshots/traces: `output/playwright/historical-surface/`. These are local generated evidence, not deployment or real-device checks.
- First approved-migration focused run: 9 executed, 5 passed, 4 failed, 3.9 minutes. Passed: all 16 property keys with existing native/dependency interactions, trash/restore, four-origin entry/Map read-only behavior, six scaffolds with one native insertion and Undo/Redo each, ghost/selection/storage/history invariance.
- Second complete run: 58 executed, 52 passed, 6 failed, 0 skipped, 14.0 minutes (`output/playwright/historical-current-summary.json`). The late-approved date-helper and close-label test changes were not in that run's loaded test snapshot. Follow-up six: 5 passed / 1 failed; the remaining date case passed after correcting command-versus-event instrumentation. These are separate execution counts, not a retroactive 58/58 full-run claim.
- The first final-run attempt was explicitly interrupted after 19 passes so the newly confirmed Plan error-focus defect could be fixed and rebuilt. It is not a completed 58-case result. The error summary selector pointed to a non-rendered attribute; the root fix uses the actual shared `[data-editor-error-summary]` owner, retains draft/storage failure checks and restores the browser's actual focused-summary assertion. The rebuilt focused stale case passed 1/1 (`output/playwright/historical-focus-final-summary.json`).
- Final rebuilt historical Surface run: **58 executed / 58 passed / 0 failed / 0 skipped**, one worker, retries zero, 7.2 minutes. All seven historical specs ran together, including Stage 3's 13 cases and the actual stale-error focus regression. JSON: `output/playwright/historical-final2-summary.json`; generated artifacts: `output/playwright/historical-final2/`. This result supersedes the intermediate execution statuses above only for the preserved historical owner, not original-requirement equivalence or current Program completeness.
- Remaining first-focused failures: standalone navigation includes the approved separate draft lane; Escape from a property value now returns to its owning group; added geometry test initially missed the existing resize-cancels-move contract. Tests are being corrected against these contracts, not weakening hit, storage or canonical-field assertions.
- Genuine legacy Surface defect: the sticky move-panel heading could cover a target after `scrollIntoView({block:'center'})` at 844x390. The Surface now measures actual header height for scroll padding; if a long heading/error leaves insufficient room in a short viewport, that heading scrolls with content. No state/store/transition changes. The second full run case 16 passed: original 844x390 target hit/full bounds, presentation-only long-title/error-copy stress at 844x300 and 375x812, resize cancellation/reopen, restored sticky header at 844x390, and unchanged storage. Synthetic rendered-copy stress is not a claim of real-device or observed-user validation.
- React and standalone editing have two input/result steps plus a draft-library lane. React puts the draft action outside `작성 단계`; standalone has three total buttons in `작성 화면`, with exactly two editing `data-step` values and one `drafts` value (HTML:32433). The test asserts these exact sets rather than silently accepting arbitrary additional steps.

No result here establishes current Program UX parity, real Android/iOS behavior, or observed-user validation.

## Additional approved owner-contract migrations

- Stage 1 duplicate save: the current receipt-only branch removes the save form (`PersonalWorkspacePocAuthoringSurface.tsx:3761-3766`), as the existing parity test also requires. The test now performs an actual save-button double click, retains atomic write-order assertions and requires exactly one authored Flow, then asserts receipt-only/save-button absence plus Enter/Space on the receipt heading produce zero further writes. The old visible-button retry's neutral status is **not** reported as equivalent to the current success receipt. Direct identical-handoff retry is still covered by `personal-workspace-poc-state.test.ts:935-974`, and storage true-no-op by `personal-workspace-poc-storage-transaction.test.ts:101-124`.
- Stage 4 focus: `k2c-placement-and-adapter.md:16-18` distinguishes pointer result visibility from keyboard return focus. Success paths now use actual keyboard focus+Enter and retain exact semantic-owner focus; cancel focus stays unchanged. An additional pointer success requires the local Undo result to be visible, in viewport and hit-testable without forcing focus to Undo. The initial pointer-focus failure is an expectation drift, not an established product focus defect. It does not establish original unconditional pointer-focus parity or actual assistive-technology behavior.
- Stage 2 entry: the actual `PersonalWorkspacePocEntryPreview.tsx:90-96` now reuses ResultPresenter. The test keeps the same Todo→Item→Map-child reset/read-only scenario using its exact `personal-workspace-result-view-*` tabs and `aria-selected`, instead of removed entry-specific buttons/`aria-pressed`.
- Stage 2 date helper: `k3a-existing-regression-qa.md` section 2 and section 3 case 24 explicitly record the later item-information→group path; `k3a-design.md` section 5.1 requires explicit value apply. The current test checks empty-date apply makes zero raw/draft/storage/native-input mutation, then applies `2026-09-21` as one native insertion with exact syntax, Undo/Redo and the existing invalid-date exact-line review return. The removed helper's immediate **blank** date syntax insertion is not claimed equivalent or restored; this is a recorded approved contract change, not a newly completed original requirement.
- Native insertion instrumentation counts actual `document.execCommand('insertText')` invocations: empty apply 0, valid date apply exactly 1. A single multiline native command can emit more than one native input event; `PersonalWorkspacePocLiveEditor.test.tsx:321-327` explicitly covers that contract. The first added event-count assertion was a test error, not evidence of two product transactions. Exact source bytes, one-step Undo/Redo and real native input events remain required.

## Standalone artifact assertion migrations (2026-09-20)

These changes apply to the pinned `2026-09-02-flowme-integrated-flow-poc-android-single-file-ko.html` artifact. They do not alter that historical HTML, establish original D2/M4 equivalence, or certify the current Program product.

| Original assertion / failure | Approved contract and retained checks |
| --- | --- |
| Template selection immediately inserts text; earlier example title fixture | K3-A preview/explicit-apply contract. Selection preserves exact empty raw text, all storage bytes, zero storage calls and zero native insertion calls. Preview equals the canonical example's complete bytes. Explicit apply produces exact scaffold and one native insertion; existing native Undo/Redo and unavailable-native failure checks remain. Original immediate-insertion equivalence is not claimed. |
| State/membership/order read from legacy key; only one low-level save call | `k2b-checkpoint-contract.md` and `k2b-action-journal-addendum.md`: read validated version-2 checkpoint, fixed v2 journal, exact prepared/target/confirmed/journal-remove sequence. Legacy bytes remain unchanged for ordinary operations. Six Flow movement paths plus Undo produce 12 successful transactions / 48 storage calls, not 48 user changes. |
| Corrupt draft mounts an empty editor | K2-B feature quarantine. Require corruption message, editor absence, exact corrupt raw and entire storage preservation, and zero storage mutation calls. This stronger failure gate is a changed screen contract, not the original empty-editor surface. |
| Four initial rows irrespective of execution date | Seed dates are 2026-09-02, while the later timeline uses local today and includes overdue items. Freeze Date to 9/2 without freezing timers; require exact original four IDs. Reorder the three untimed items while preserving the timed meeting. No arbitrary 4-to-7 count replacement. |
| Drag, Undo, then keyboard reuses the open move owner | Contextual Undo restores the move owner with its previous ticket. Close it with Escape before a fresh keyboard operation. Require identical resulting order and stored reference keys; stale-ticket behavior is not silently presented as original sequence equivalence. |
| Edge-scroll assumes centering the source leaves the last row offscreen | Measure and position the real source in view and last target below the viewport, then require actual edge scrolling, offscreen-to-visible movement, browser drop, exact order, Undo and operating-byte invariance. No task/state injection. |
| Result sheet assumes fixed cell indexes | Use exact semantic headers `원 발생일` and `실행 날짜`; retain original-source date versus personal execution-date checks after occurrence columns were added. |
| Permanent deletion treated like ordinary workspace write | The action-journal addendum explicitly permits owner-scoped new/legacy/source candidate targets only for deletion. Audit ordinary calls before deletion separately. This seed writes new workspace once and legacy once; absent source candidates stay null with zero writes. Journal calls remain exact-key scoped; no prefix wildcard or clear. |
| Draft cleanup failure immediately rolls back in catch | Addendum sections “성공·실패·재시작” and “handoff와 reset의 차이”: prepared failure remains quarantined, with zero successful changes and exact draft/legacy bytes. Require exact handoff journal entries, reload preserving all storage bytes, then explicit recovery restoring workspace/draft before bytes and removing the journal. Reload checks byte invariance; it is not a direct zero-call counter assertion. |
| Sixteen property rows and global cancel announcement | K3-A four-group chooser has exact group counts 6/3/2/5, with the canonical 16-key union and no duplicates. Open the scheduling group before duration editing. Inline cancel returns to the same property group/opener rather than a global status (`HTML:32650-32664,34084`); original raw, storage and native-input zero-change checks remain. |
| Successful completion must show a global toast | K2-C local result ownership (`HTML:29249,32515`) uses the active contextual result. Force the same four safe-area insets and require actual active-result bounds plus visible/hit-testable Undo. This tests the current local feedback geometry, not original global-toast parity. |

### Standalone execution and evidence limits

- Original complete artifact/standalone baseline: 14 files, 51 executed, 31 passed / 20 failed / 0 skipped, 509.5 seconds. `output/playwright/artifacts-merge/results.json` preserves the initial failure messages and stacks.
- Evidence handling limitation: a later P1 targeted invocation reused the baseline artifact output directory. Playwright cleaned some initial screenshots/traces. Do not claim a complete retained initial trace set; the initial JSON remains. All subsequent focused final runs use distinct output directories.
- Focused final runs, counted separately rather than combined into a full-run claim: template/storage/fail-closed 6/6 (`artifacts-template-final/results.json`); drag/context 3/3 (`artifacts-drag-final/results.json`); P1/recovery 2/2 (`artifacts-p1-recovery-final/results.json`); Flow movement 1/1 (`artifacts-flowdrag-final/results.json`). These paths are under `output/playwright/` and are generated local evidence.
- The initial Flow membership failure was an obsolete legacy-key read, not evidence of a native-drag implementation defect. After validated-v2 migration, all six movement paths passed without modifying the HTML.
- Final single run after all owners finished: **51/51 passed, 0 failed, 0 skipped, 0 flaky, exactly 51 attempts across 14 files**, one worker and retries 0. Started `2026-09-20T14:39:39.935Z`; duration 235.591 seconds. JSON: `output/playwright/artifacts-final-all/results.json`; isolated artifacts: `output/playwright/artifacts-final-all/artifacts/`. Command: `npx.cmd playwright test --config output/playwright/artifacts-final-all.config.ts` (local ignored config selecting the manifest's complete artifact/standalone groups). This is separate from all earlier focused runs.
- Post-run `git diff --numstat` for the pinned Android single-file HTML and `2026-09-04-flowme-integrated-poc-scenario-runner-ko.html` was empty. Generated tracked PNGs remain for root's exact-path evidence backup/restoration; they are not HTML source changes. Browser automation is not Android/iOS device testing or observed-user validation.
