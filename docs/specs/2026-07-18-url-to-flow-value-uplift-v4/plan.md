# Plan

## Objective

Replace “valid SourceRow compilation” as the success definition with a source-grounded product-quality gate that can reject thin inputs and compare usable Flow candidates with the live Flow 찾기 baseline.

## Work sequence

1. Capture the seven live `/flows` entries and identify the strongest pattern references.
2. Audit the ten v3 positives for copy depth, completion, source locator, schedule, and projection payload.
3. Define evidence ownership, source sufficiency dispositions, safe value licenses, and hard fail codes.
4. Encode a deterministic validator and negative self-tests.
5. Reassess the same 12 cases without changing their historical evidence.
6. Re-fetch one bounded official source and build a worked candidate.
7. Prepare randomized A/B packets and run two independent reviewer proxies.
8. Generate and visually verify the Korean HTML deck and case previews.

## Stop conditions

- Do not call a thin SourceRow a Flow merely because its JSON validates.
- Do not change or delete frozen v3 evidence to improve the v4 story.
- Do not claim a public-ready generator from one worked example.
- Do not select a cheap or premium extractor without real token, latency, cost, edit, and accepted-Flow evidence.

