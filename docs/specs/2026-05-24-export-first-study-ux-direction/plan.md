# Plan

## Implementation

1. Record the agreed UX/UI direction in durable product principles.
2. Add a study UX gap audit focused on source-derived curriculum structure.
3. Add a common first-screen UX audit across current representative/public-MVP routes.
4. Add RED tests for study default progress rows and export behavior.
5. Extend artifact log rows with editable default values.
6. Render source-derived defaults in the study log table and include them in text/xlsx exports.
7. Capture desktop and mobile screenshots.
8. Run full verification, open PR, and merge if checks pass.

## Review Checkpoints

- Do not imply automatic URL ingestion exists yet.
- Do not make FLOW a native study dashboard in this PR.
- Defaults must be editable and exportable, but still framed as source-derived working rows.
- Keep the change small enough to review as a UX direction PR, not a platform feature.
