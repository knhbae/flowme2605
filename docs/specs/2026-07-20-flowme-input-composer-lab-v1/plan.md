# Plan

1. 기존 canonical 규칙과 Round 4 source-backed 8개 사례를 동결한다.
2. Item 조합, 입력 소유권, progressive disclosure, projection 계약을 JSON으로 고정한다.
3. upstream canonical entity를 복사·발명하지 않고 Composer scenario를 생성한다.
4. 첫 화면부터 실제 입력과 결과물이 보이는 self-contained 한국어 HTML을 만든다.
5. schema, invariant, negative fixture, browser interaction, desktop/mobile visual QA를 실행한다.
6. 자동 QA와 관찰 사용자 검증을 구분해 인수 결과를 기록한다.

범위 밖: app/runtime 타입 변경, DB migration, provider API, crawler, commit, push, PR, merge, deploy.
