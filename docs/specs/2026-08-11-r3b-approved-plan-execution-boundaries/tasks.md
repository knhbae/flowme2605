# R3B Tasks

## Baseline

- [x] Confirm clean named worktree, branch, HEAD, upstream, and dirty ownership.
- [x] Record current `AppClient` and `MyFlowRuntime` concentration.
- [x] Confirm owner authorization for refactor, commit, push, PR, merge, and deploy.

## R3B-00

- [x] Implement the hashed approved product contract across public preview,
  My Plan, Calendar, responsive help/warning, duplicate display, and export.
- [x] Reuse shared sort, date-group, raw-memo, responsive, and export-scope
  mappings without adding a content-specific persistence owner.
- [ ] Prove the product delta through the pending full Playwright, GitHub CI,
  exact-source deployment, canonical smoke, and remaining release gates in
  R3B-04.

## R3B-01

- [x] Add typed `MyPlanExecutionSurface` model/actions/renderer contract.
- [x] Replace the approved selected-Plan renderer without DOM or behavior drift.
- [x] Add focused component tests.

## R3B-02

- [x] Add pure approved saved-transfer request/revalidation controller.
- [x] Integrate it while keeping runtime artifact and receipt effects in
  `AppClient`.
- [x] Prove TXT/VTODO/VEVENT/XLSX identity, bytes, and receipt ordering.

## R3B-03

- [x] Characterize the approved and compatibility Item detail/editor branches.
- [x] Stop this slice because the current 2,074-line renderer crosses ten call
  contexts and would require a mega prop bag or duplicate view model.
- [x] Record approved read-only Item detail extraction as a separate,
  unpromoted future candidate; it is not an R3B completion item.

## R3B-04

- [x] Migrate stale URL-first E2E journeys without deleting unique coverage;
  the full URL-first suite passes `20/20`.
- [x] Run approved targeted tests `176/176` and full `npm test`; both pass.
- [x] Run storage-lock `59/59` and security audit `0`; both pass.
- [x] Add the draft PR history with unreached publication states as `NOT_RUN`.
- [x] Run `npm.cmd run verify`; docs `16/4492`, full `npm test`, and the
  production build `18/18` pass with BUILD_ID
  `OLhvDcxZwIjvVgL9bhJ7n`.
- [x] Re-run docs check and diff check after documentation reconciliation.
- [x] Run the canonical browser gates: URL-first `20/20` plus approved
  plan-execution `23/23`, total `43/43`.
- [x] Preserve overlapping scoped migration evidence: Group B `33/33`, P26
  `40/40`, D1 `68/68`, P28-P31 `37/37`, P35 saved library `18/18`, Q3
  `12/12` plus off-lane `7/7`, 50-Item `1/1`, legacy transfer `26/26`, and
  Calendar `2/2`.
- [x] Complete the full repository Playwright suite: `569/569`, failures `0`,
  skips `0`, flaky `0`, workers `2`, exact BUILD_ID
  `OLhvDcxZwIjvVgL9bhJ7n`.
- [x] Run diff/ownership/rollback audit and closeout; evidence PNGs were
  restored to `HEAD`, generated `test-results-*` paths are excluded from
  publication, and no product P0/P1 remains.
- [ ] Commit, push, open PR, wait for required CI, and merge.
- [ ] Verify Vercel Production source SHA and canonical-alias smoke.
- [ ] Record release evidence; keep observed-user validation separate.
