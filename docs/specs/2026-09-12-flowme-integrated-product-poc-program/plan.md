# 전체 실행 순서

<!-- alpha-transition-20260920:start -->
## 현재 후속 계획 — 실사용 알파 전환

PoC 이후의 실행 순서·요구별 상태·완료 조건·사용자 결정은 [실사용 알파 전환 원장](alpha-transition.md)에서 관리한다. 다음 구현 묶음은 M1 저장·권한·복구 경계이며, 현재 문서 목표 완료와 M1 구현 완료를 구분한다. 아래 PoC 실행 계획과 평가 시점의 외부 작업0 기록은 그대로 보존한다.
<!-- alpha-transition-20260920:end -->

## 9/20 실행 종료

2026-09-20. 승인된 기능형 통합 PoC의 구현과 서로 다른 목적의 두 평가·개선 루프를 마쳤다. [최종 평가](final-evaluation-2026-09-20.md), [S01–S10/두 루프 근거](final-scenario-ledger-2026-09-20.md), 캡처 보고서 (로컬 전용 근거: `../../content-audit/2026-09-12-flowme-integrated-product-poc-review-ko.html`)가 최신 판정이다. 아래의 ‘현재·미완료’는 각 이전 실행 당시의 기록이며 최신 판정을 대신하지 않는다.

복구 배치·Redo 손실 수정 → 영향 회귀와 최종 build → 같은 자료의 두 탭/정렬 재검사 → 공개 파일 복귀 포커스 수정·재검사 → F-note 철회 → 최종 S01–S10 핵심 연결 → 요구별 평가·캡처 보고까지 수행했다. 성공한 자료를 재생성하지 않았다. 새 기능을 추가하며 종료선을 바꾸지 않는다.

기능형 PoC 완료와 출시·상시 실사용 준비는 다르다. 긴 화면/누적 응답·COUNT 안내 개선, 전체 백업/업데이트 이관·기기 간 보존은 별도다. 실제 Android/iOS·OS 입력기·보조기술·외부 import·관찰 사용자 검증은 미실행이다. 보고서 렌더는 URL 보안 정책으로 미실행이며 정적 검사/앱 캡처와 구분한다. commit·push·PR·merge·Preview·Production·DB/계정 가입·외부 업로드는0이다.

## 현재 실행 — 전체 상황 연결과 비교·취소 경계

2026-09-15 추가: 주간 작성 틀→명시 적용→Undo/Redo→저장120확인 뒤 이전 초안으로 돌아가는 reload 결함을 재현하고 수정했다. [실패와 후속 원장](whole-loop-two-review.md#L3)의 동일 저장본으로 pIfd 실행판의 같은ID·reload·Back/Forward166확인을 마쳤고 현재398이다. 다음은 이 자료의 시간대/하위 체크·개인 실행, 실제 Map 계획과 구성 왕복이다. 현재 native 미반영 원문 복구와 옛 D2 임시복구본 인계의 차이는 [요구 대조](creator-recovery-fidelity-review.md)·[설계 초안](legacy-recovery-handoff-plan.md)의 순서대로 검증/검토/구현한다. 이를 현재 자동보관이나 native 이력 복원으로 대신 충족 처리하지 않는다.

[현재 체크포인트](current-checkpoint.md)와 [전체 실행표](final-whole-loop-plan.md)를 따른다. [두 번째 목적 평가](whole-loop-two-plan.md) A의 무저장3파일은 fHX에서 마쳤다. B의 긴 독립 기록·폴더·정확 참조는381→388의7회 거래로 연결했고, Back 후 편집기가 숨던 결함은 Z4의 실제111확인으로 수정 검증했다. 원래 개발2 A의 전체 이력 명시 복원/Undo와 B의 개인 기록/참조2개 왕복119·반응형218은 [별도 원장](s10-legacy-creator-gap.md)에 기록했다. 한 줄 참조의 원본 이동 메뉴도 연결했다. 다음은 같은 자료를 보존한 B의 반복 연습·다른 Map 구조 → C의 선택 공유·질문/지식 → D의 복합 개인 충돌/CAS → 같은 최종판 S01~S10 대조다. 기존 실패와 초기 결과 미수집 기록은 지우거나 통과로 바꾸지 않는다.

Map 최적화는 검증 진입마다 원래 그룹과 모든 revision을 다시 검증한다. 검증 생략·전역 mutable 객체 신뢰·자료 축소·이력 삭제는 하지 않는다. 호출 내부에만 존재하는 검증 결과로 effective·review·적용 결과·Undo를 각각 판정한다. 깊은 group view 재사용은 이 단계의 측정과 child별 rebase 차이를 확인한 뒤 결정한다. 브라우저 검증 중 기존 서버를 바꾸지 않으며 새 소스의 결과를 과거IW8 완료로 표시하지 않는다.

편집기 진행 버튼의 경계 clipping 수정과 별도로, 긴 목록은761px 이상에서 독립 스크롤을 사용하고 페이지 자체의 수동 스크롤은 유지한다. navigation callback에 강제 focus/scroll을 추가하지 않았다. 한 줄 참조 메뉴는 기존 원본 패널/저장 보호 경로를 재사용한다. 844×390의 작성 영역 약70px·참조 패널 내부 스크롤, 좁은 화면 재배치 후 커서 행 가시성은 후속 사용성 확인 대상이다. Map 실행 제외는 기록을 보존하고 개인 문서·폴더·원문 접근을 유지한다. 같은 최종판 전체10상황·다른 목적의 두 번째 전체 평가/개선, 미해결 npm 출처기한1건·의존성5건은 그대로 남는다. 새 실행·전체 백업·알림·DB·계정·배포 등 미승인 제안을 필수 범위에 추가하지 않는다.

### S08 이후 현재 묶음

S08의 IW8 최초30확인과 같은371의17확인 후, 기존 공유 UX SL11/P07의 일반 충돌 선택을 추가했다. b9n의 모델9·UI6개 새 검사와 전체1646검사·build, 같은 사본의 취소/실패/수용/Undo41단언으로375를 확인했다. Map 성능·실행 제외 화면은 별도 기존 프로필의4→7로 재검사했다. 기존 입력을 초기화하지 않고 S09~S10과 다른 목적의 두 번째 전체 루프를 잇는다.

## 이전 실행 — 작성 틀 결과 정책 연결 뒤 전체 입력 대조

[원래 결과 규칙과 실제 여행 대조](creator-template-result-review.md)에서 첫 결과와 Sheet 제공 조건을 raw/native 화면에 연결했다. 실제 여행15확인·5크기·저장0,155파일1521/strict349/build를 확인했다. 첫 입력 검사는 새 빈 초안137까지 만든 뒤 QA selector 오류로 멈췄고 같은 초안을 보존해 재개했다.

다음은 여섯 원래 fixture의 모든 폼 필드→미리보기/취소→한 native 적용→Undo/Redo→저장/reload와 원래 결과를 실제로 대조하고, 해당 작성물을 개인 실행/선택 공개까지 잇는 일이다. 그 뒤 실제 Map 삭제·개인 충돌/복잡한 전환, 긴 화면/응답과 같은 최종판 S01~S10·두 전체 개선을 종결한다. 작성 틀 표시 정책을 영구 제품 정책이나 운영 schema로 승격하지 않는다. 아래는 이전 실행 순서다.

## 현재 실행 — 체크 연결 검증 뒤 원본 기능 동등성

[하위 체크 제안·복원·순서](copy-check-resolution-review.md)를 실제 v6~v9와 개인 완료/문구/Undo로 확인했다. JH0 최종20확인/재개6확인·5크기·1499검사/strict346/build·375소스42대조를 남겼다. 커서/순서 결함과 QA Undo 가정 오류를 분리했다.

다음은 여섯 작성 틀의 원본 필드·예시·조건·편집/실행/공개를 한 행씩 대조하고, 실제 Map 항목/하위 Flow 삭제와 지난 기록을 원본 변경 owner에서 검증하는 일이다. 개인 충돌·복잡한 종류전환/체크 하위 구조·미정 시작 조합은 잔여로 유지한다. 새 실행/백업/재공개 정책을 열거나 쉬운 합성 예시로 원본 동등성을 대신하지 않는다. 그 뒤 긴화면/누적응답과 같은 최종판 S01~S10·두 전체 평가/개선으로 종결한다. 아래는 이전 실행 순서다.

## 현재 실행 — 종류 전환 검증 뒤 하위 체크 충돌과 원본 동등성

[일반↔반복 기록 복원](copy-kind-change-review.md)을 구현하고 같은 실제 공개5판으로 최종100→112의24확인·두방향각5크기·여섯Undo/reload를 마쳤다. 미정 상속값 복원 결함도 수정했고1472/1472·strict343/build T5O·372소스/저장40대조를 남겼다. 이를 전체 종류 변경 조합이나 전체 두 개선 루프 완료로 확대하지 않는다.

다음 순서는 (1) 재추가 하위 체크의 원래 행·개인 수정·완료/참조 보존을 대조하고 명시 충돌 해결을 설계·구현·검증, (2) 여섯 작성 틀과 실제 새 Map의 삭제·다른 구조/과거 기록 등 원본별 미충족 종결, (3) 같은 최종판 S01~S10과 전체 평가→개선 두 차례다. 복잡한 종류 전환의 개인 하위 구조, 미정→다른 미정과 현재 판본 개인 계획의 추가 변경, 긴 화면/누적 응답은 해당 단계에 함께 포함한다. 원래 기능·UX와 연결·안전 계약을 같이 확인하며 새 실행/백업/재공개 정책이나 운영 발행으로 범위를 바꾸지 않는다.

## 이전 실행 — 시작 조건 복구 후 종류 전환·하위 체크 충돌

[같은 자료70→85의 시작 조건 검증](copy-start-followup-review.md)에서 과거 계획이 새 시작일을 막던 결함을 수정했다. 현재 `XKvQ2x05jmRnGKt8im28g`, 신규7포함1461/strict341/build, 수정 전4실패→표적48통과·실제 미정13/상대16확인·370소스와 저장39대조다. 아래 개인 시작·계획 충돌 전체 미연결은 이전 판정이다.

다음 순서는 (1) 일반↔반복 종류 전환의 기록/원문/참조 보존 설계·구현·회귀, (2) 재추가 하위 체크의 개인 수정 충돌, (3) 여섯 작성 틀·실제Map 삭제 등 원본별 미충족, (4) 같은 최종판 S01~S10과 전체 평가→개선 두 차례다. 각 단계에서 원래 UX·데이터 계약·실제 시나리오를 함께 검증한다. 미정→다른 미정의 기존 시작 유지·현재 판본 개인 계획 이후 시작 변경, 긴 화면/응답도 별도 잔여다. 전체 목표·운영 경계·미승인 정책을 축소하거나 바꾸지 않는다.

## 이전 실행 — 전환·개인 계획 충돌과 남은 원본 동등성

[같은 작성물의 후속 검증](authored-update-loop-review.md)에서 설명 중복과 B 제안→불변2판→일정만 수용→Undo/reload를 종결했다. 현재kG, 신규1443/strict341/build, 실제 출력21·후속27·저장 기록33대조다. B는64상태로 보존하며 일반↔반복·개인 시작일/전체·이후 계획 충돌, 실제Map 삭제·여섯 틀/긴 화면, 최종 같은 판 S01~S10과 두 전체 개선 루프를 계속한다. 새 정책이나 운영 발행으로 범위를 바꾸지 않는다.

## 이전 실행 — 실제 작성물의 후속 변경과 출력 결함

[현재 결과와 다음 순서](recurring-publication-release-review.md)가 아래 FDEz 당시 계획을 갱신한다. 지원 반복의 실제 공개·A 제안 채택/개인 수용을 연결했고 B 작성 초안→공개→새 사본→완료·개인 날짜/기간·Undo→3파일/정확 복귀를 같은 E1에서 확인했다. 다음은 B의 같은59번 저장 상태에서 설명 중복을 개인 메모 보존 조건으로 수정하고, 후속 제안→새 판본→부분 수용→Undo/reload를 종결한다. 이어 일반↔반복/개인 시작·계획 충돌, 여섯 틀/실제 Map 삭제/긴 화면, 전체10상황·두 전체 개선 루프를 계속한다. 새 정책·운영 발행으로 범위를 바꾸지 않는다.

## 이전 체크포인트 — FDEz 입력 연결 직후

현재 작업: [FDEz 반복 제안 입력·실제 제출](recurring-proposal-input-review.md)을 연결했다. 기존 사본의 typed 반복/기준판본·항목/요청 identity·미제출 입력 보호, quota 실패→같은 요청1제안·검토/원문 복귀/reload를 확인했다. 전체1434/strict341/build, 부분21/10과 최종14확인·5크기 및370소스20대조를 구분한다. 다음은 현재 실제 반복 제안을 보존한 채 작성자 채택→새 불변 판본→개인 사본 명시 수용을 이어 검증하고 공개 제한을 여는 일이다. 일반↔반복·개인 시작/계획 충돌 해결과 E의 실제 작성→공개→사본→실행→출력→변경 수용→Undo/reload도 계속한다. 입력·제출을 반복 공개 전체 완료로 세지 않으며 전체10상황·두 전체 개선 루프, 여섯 틀/Map/긴 화면의 범위를 축소하지 않는다.

2026-09-12. [범위](spec.md)의 P01~P08 모두를 끝내기 위한 순서다. 각 행에서 기획/UX·데이터 설계·구현·검사를 함께 진행한다.

| 순서 | 작업 | 중간 완료 조건 |
| --- | --- | --- |
| 0 | 기준선·소유·재사용·연구·검증 기준 | 기존 main/PoC/v11 차이, 보호 manifest, 필수 잔여와 새 요구 원장 |
| 1 | 개인 문서 모델·통합 transaction·실시간 editor adapter | 원문·안정 ID·날짜 누적·참조/이동·저장 실패를 단위/순수/화면에서 확인 |
| 2 | 개인공간 shell·문서/폴더/기간·기존 Flow 연결 | 동일 target, 기간 중복0, 정확한 작성 위치·Undo·reload, 기존 작성/실행 기능 재진입 |
| 3 | 공개 repository·탐색·개인화·URL·출력 | 기존 source 조회와 미지원 구분, 부분 사본/기준일/문서 연결, 저장 없이 실제 파일 |
| 4 | 경험·질문·답글·반응·근거 요약·내 활동 | 로컬 쓰기/수정/삭제·중복/실패/복귀·사진 선택/공개 제외 |
| 5 | 선택 공개·초안 이력·파생·제안·판본·업데이트 | 공개 whitelist, 실제 새 판본, 사본 비교/유지/부분 적용/충돌/Undo, 지난 기록 불변 |
| 6 | 첫 전체판 통합 평가와 개선1 | 기본 10개 상황과 원본 매칭, 실제 결함/부담을 기록하고 수정·회귀 |
| 7 | 추가 상황 평가와 개선2 | 다른 내용 구조/목적, 빈/긴/실패/삭제/다중문서/키보드, 새 문제 수정·회귀 |
| 8 | 전체 QA·화면 캡처·보고서 | 전체 범위 trace, npm/build/보호 검증, 허용 브라우저, 실행 방법·짧은 요약+접힌 상세 |

독립 모델/커뮤니티/출력 구현은 공유 계약을 먼저 고정한 후 파일 소유를 나눠 병렬화한다. 기존 파일은 root의 사전 diff 확인 없이 각자 고치지 않는다. 단계 번호나 테스트 수만으로 전체 완료를 선언하지 않는다.

## 다음 실행 묶음 — 반복 작성물의 공개 구조 손실부터 처리

사본 import/참조 조사에서 숫자 회차 key가 공통 순서 검사를 통과하지 못하는 선행 결함을 재현했다. [출력 복귀 연결](public-copy-output-return-review.md)에서 이 key와 수용한 일정 판본을 고정한 복귀 reader를 먼저 보완했다. 준비용 계약 검사이며 실제 사본 저장 gate는 유지한다. 다음은 아래 C3의 시작 미정·header/하위 확인·다중 문서 참조와 나머지 D consumer를 연결한 후 실제 저장·왕복을 여는 일이다.

후속 [C 개인 계획 모델](public-copy-plan-review.md)은 authoring-v1의 지정 첫 시작과 반복 의미를 유지하면서 전체/이후 계획·지난 기록·공통 기간·순수 Undo를 연결했다. 새21/strict327/0, 최종1,330/build e-s6와 같은 기존 초안15확인을 구별한다. 실제 사본 저장/시작 미정/header·하위 확인/출력·제안·재진입과 사용자 왕복은 남는다. 다음은 일반 Item 중심의 import/참조/기준일 접점을 반복 owner에 연결하고 모든 consumer 준비 뒤 gate를 여는 일이다. 모델 검사를 실제 공개/사본 사용자 기능 완료로 세지 않는다.

후속 [C 공통 읽기·회차 대조](public-copy-recurrence-review.md)는 실제 공개 판본의 필드별 수용과 별도 사본 tuple·공통 기간 reader/기록 보존까지다. 새17 포함 표적41·strict325/0을 확인했다. live public store와 import/개인 전체·이후 계획/출력·제안/재진입은 아직 미완료이며 아래 C/D와 E를 그대로 이어간다. 준비용 계약 fixture를 실제 공개·사본 생성으로 세지 않는다.

현재 [B 초안 UI 결과](recurring-publication-draft-review.md)의 같은 실제18확인과2반복/1보존메모·실패/재시도/reload를 완료했다. 전체1,291검사 뒤 fieldset 잠금을 보완하고 최종 표적87/strict323/build·같은초안15확인을 마쳤다. 다음 큰 묶음은 C/D 실제 공개 사본 owner·회차/계획/기간/출력/제안이며, 모든 consumer가 준비된 뒤 공개 store를 열어 B의 불변 판본과 E의 연속 시나리오를 함께 종결한다. 아래 A 이후 항목은 이 현재 순서로 갱신해서 읽는다.

1. [A~E 연결 설계](recurring-publication-design.md)를 이어간다. A 원본 읽기와 B 초안의 명시 정리는 구현됐으며 실제 초안은2반복/1보존메모다. C의 공개 사본 공통 reader는 모델 검사까지다. 다음은 실제 저장/불변 판본·개인 사본 import/전체·이후 계획과 D 표시/출력/원본 비교, E 같은 자료의 연속 검증이다. 원래 task0/note9는 수정 전 이력으로 구별하며 단순 메모 발행이나 고정 회차로 반복 원본을 대체하지 않는다.
2. [복구 UI29확인](responsive-recovery-review.md)과 [Map 새 항목18확인](map-catalog-change-review.md)은 완료한 해당 범위로 유지한다. 두 프로필의 원문·초안·추가 완료·과거 기록을 초기화하지 않는다. 이 검사 반복을 전체 개선 루프로 세지 않는다.
3. 실제 새 상위판본의 Map 삭제·다른 반복/과거 기록, 여섯 작성 틀 전체 동등성, 가로 화면·긴 문서·누적 자료 응답성을 이어 종결한다.
4. 전체10상황의 현 실행판 연속 확인·두 전체 개선 루프와 최종 캡처 보고를 수행한다. 실기기/외부계정/관찰·발행/배포·보고서렌더 경계와 미확정 정책은 유지한다.

## 이전 실행 묶음 — 남은 기존 기능과 긴 화면을 함께 종결

1. [여행 원장](creator-mobile-travel-review.md)의 동일 자료·새 공개 판본·개인50%/메모를 유지한다.17/7/9/11확인과 Map 모델2개,검사 코드 오류를 구분하고 해당 여행 전체 경로를 다시 미구현으로 세지 않는다.
2. [Map 목록 변경](map-catalog-change-review.md)의 가상 과거 목록→실제 현재factory 수용→새 항목 명시 선택·기록·원본/전역Undo를 허용된 UI로 검증한다. 실제 새 상위 판본의 삭제는 별도이며 원본snapshot을 직접 덮어 정상 갱신으로 간주하지 않는다.
3. 여섯 작성 틀의 남은 시험 등 구조와 지원 원문을 기존 기능별로 종결한다. 동시에 캡처에서 확인한 공개 제목 줄바꿈, 긴 복구 footer, 상단 메뉴 때문에 좁아진 개인 편집 영역·이중 스크롤·누적 자료 응답을 보완한다.
4. 전체10상황의 현 실행판 연속 확인과 처음 쓰지 않은 목적/구조의 두 번째 개선을 완료 조건에 맞춰 남긴다. 부분 재검사 수를 전체 두 루프로 계산하지 않는다. 실기기/외부계정/관찰·새정책·발행/배포·보고서렌더 경계는 유지한다.

## 완료한 실행 묶음 — 빈 원문 첫 초점·모바일 작성 틀과 여행

원 P0 화면 계약에 따라 새 빈 초안을 연 성공 시에만 원문에 첫 초점을 둔다. 기존 저장본 열기·reload·취소/저장 실패·IME에서 초점을 덮어쓰지 않는다. 작성 틀은 PC에서 현재 pane을 유지하고 모바일에서는 사용자가 여는 한 열의 native dialog drawer로 표시한다. 동일한 폼/sidecar를 유지하며 닫기·회전이 입력을 삭제하거나 저장기를 추가하지 않는다. 적용 후 원문과 저장 결과로 복귀한다.

1. 원문/폼의 현재 캡처와 writer·focus 소유 코드를 대조한다.
2. 부모의 성공한 빈 초안 진입 focus와 모바일 panel만 보완한다. 기존 원문 편집/Undo·동일 폼을 재사용한다.
3. 취소·IME·회전·포커스·저장 실패·단일 적용/재진입과 새 여행/시험 구조를 검증한다. 실제 기기 키보드가 없으면 작은 viewport 검사를 대체 실기기 증거로 부르지 않는다.
4. 자동 회귀·build·허용 브라우저·캡처와 원래 요구별 충족 상태를 갱신한다. 여섯 틀 전체·선택 공개·Map·전체10상황/두 루프의 잔여는 유지한다.

## 이전 실행 순서 — v-SE 조건별 입력과 반복 개인 실행 뒤

1. [조건별 입력 원장](creator-structure-conditions-review.md)의 완료17/11·부분5/5/2, 현재revision30과 이전 자료를 보존한다. 원문과 개인 문서 투영문, 임시 runtime 배열과 현재 문서의 build 근거를 혼동하지 않는다.
2. 실제 빈 초안의 원문 첫 초점과 필수값 안내·모바일 폼을 원래 UX에 맞게 보완하고, 여행/시험의 다른 구조를 실제로 작성해 선택 공개까지 이어 검증한다. 이 주간 반복 한 경로로 여섯 틀의 모든 기능을 충족했다고 하지 않는다.
3. 기존 Map 새 항목/삭제·다른 반복/과거 기록, 긴 화면·응답성, 전체10상황과 두 개선 루프를 계속한다. 운영 데이터·새 정책·외부 게시/배포·실기기/관찰과 보고서 렌더 경계는 유지한다.

## 완료한 실행 묶음 — 작성 틀 조건별 입력과 오류 복귀

2026-09-13. 앞선 예시·계산·이력 복구는 진전으로 유지한다. 반복/여행/시험의 원래 카탈로그와 validator에 맞춰 조건별 입력을 대조한다. 선택하지 않은 방식의 **빈 필드만** 접고, 이미 입력한 값이나 오류는 숨기거나 자동 삭제하지 않는다. 검증 실패 때는 정확한 group/slot의 입력으로 돌아갈 수 있게 한다. 원문·sidecar 계약, 계산기와 운영 저장소는 변경하지 않는다.

1. 현재 실제 폼을 확인하고 여섯 틀의 조건별 필드·반복 group·전환/취소/한 번 Undo를 비교한다.
2. 원래 검증을 보존하는 표시 helper와 접근 가능한 오류 복귀를 구현하고 관련 회귀·타입·npm/build를 실행한다.
3. 보존된 프로필에서 새 실제 빈 초안으로 다른 구조를 작성하고, reload·실패·조건 전환을 브라우저로 확인한다. 기존 이사 revision18과 main revision72는 초기화하지 않는다.
4. 작성→개인 실행→선택 공개는 실제로 이어서 확인한 범위만 판정한다. Map·전체10상황·두 개선 루프와 모바일 drawer/실기기 잔여는 별도 유지한다.

## 이전 실행 순서 — p9m 작성 틀 예시·계산·이력 복구 뒤

1. [작성 틀 원장](creator-structure-guidance-review.md)의 구현과 실제33/10/6부분/2·13기록대조를 보존한다. 원문 판본과 작성 설정 판본, privateUndo와 요청영수증 보존을 혼동하지 않는다. `program-structure-form-review`의 최종revision18, 기존main QA프로필revision72를 초기화하지 않는다.
2. 원래 여섯 틀의 모든 입력·필수/조건부 필드·바꾸기/되돌리기·원문첫초점과 모바일 작업공간을 다른 반복/여행/시험 구조로 대조하고 개인 실행/선택 공개까지 검증한다. 예시6개 읽기 성공을 모든 작성동등성으로 세지 않는다.
3. 실제 Map 새 항목 추가/삭제·다른 반복/과거기록, 긴 비교/응답성과 전체10상황의 두 평가·개선 루프를 이어간다. 새 실행/전체백업/재공개 정책과 실기기/외부계정/관찰·발행/배포 경계는 바꾸지 않는다.

## 이전 실행 순서 — Z4J Undo 선택 복원 뒤

1. [Undo 선택 복원](creator-undo-selection-review.md)의 실제 수정과 19확인·실패 상태 재개8확인, 최종1212/strict313/build를 해당 범위의 해결로 유지한다. 같은 기록이 있는 최종 revision72 프로필을 초기화하지 않는다. 오류 후 전역 버튼 초점과 reload 전체 선택의 영구 보존은 이 수정의 완료 주장에 포함하지 않는다.
2. 기존 개발2의 지원 원문·작성 틀·저장 이력을 원래 control/결정별로 종결한다. 특히 빈 원문/틀 변수·명시 적용/해제·전체 저장본과 다른 원문 구조를 개인 실행/선택 공개로 이어 기존 기능 동등성을 확인한다. 이미 통과한 같은 병합·분할 반복을 새 전체 개선 루프로 세지 않는다.
3. 실제 Map 새 항목 추가/삭제·다른 반복/과거기록, 긴 비교/응답성과 전체10상황의 두 평가·개선 루프를 이어간다. 새 실행/전체백업/재공개 정책, 실기기/외부계정/관찰과 발행/배포 경계는 바꾸지 않는다.

## 이전 실행 순서 — ZI3 병합·분할과 선택 공개 검증 뒤

1. [병합·분할 원장](creator-corrections-review.md)의 실제 Undo 선택 유실을 보완한다. 부모의 외부 snapshot 동기화가 같은 owner/존재하는 Item의 선택을 유지하고, 삭제된 항목·다른 owner·dirty/IME/잠금에서는 잘못 복귀하지 않게 한다. 실제 revision66의 자료·불변 공개판본·45%/25% 기록을 그대로 이어 확인한다.
2. 이 선택 보완을 실제 분할/병합 성공·Undo·재선택·reload와 비교한다. ZI3분할11/병합18의 멈춤 기록과 선택공개21완료,최종1206/strict313/build 및15대조는 그 범위의 근거로 유지한다.
3. 다른 지원 원문·전체 제작 관리·Map새항목/삭제/다른반복·긴 비교/응답성과 전체10상황의 두 평가·개선 루프를 계속한다. 실제 기기/외부계정/관찰,새실행/전체백업/재공개정책과 발행/배포 경계를 바꾸지 않는다.

## 이전 실행 순서 — Gpl 일반 속성·개인 인계 검증 뒤

1. [속성 대조 원장](creator-inspector-addition-review.md)의 원래11속성 추가·편집 선택/초점 복원을 완료한 해당 범위로 유지한다. 같은 자료의 최종40확인과 앞선 QA 오류·최종1200/strict312/build를 구별한다.
2. 기존 개발2의 병합/분할을 원래 control·순수 연산·현재 화면·개인 연결·선택 공개별로 대조한다. NCUI18/19/20과 NL02/03은 각각 구조 조작/거절과 명시 원문 연결의 근거이며, 기록이 있는 개인 실행 및 공개의 전체 왕복을 대신하지 않는다. merge/split 뒤 원문을 강제로 재생성하거나 제목으로 identity를 추정하지 않는다.
3. 나머지 지원 원문/속성·작성 틀·저장 이력을 요구별로 종결하고, 실제 Map 새 항목 추가/삭제·다른 반복/과거기록 조합과 전체10상황의 두 평가·개선 루프를 이어간다.
4. 긴 폼/비교·모바일 줄바꿈과 스크롤·누적 자료 응답성을 함께 개선한다. 새 실행·전체 백업·전체 제외·재공개 정책, 실기기/관찰/외부 계정과 발행/배포는 별도 경계다. 전체 목표는 진행 중이다.

## 이전 실행 순서 — pO7 Map 포함 선택과 원본 수용 검증 뒤

1. [Map 포함·원본 수용 원장](map-membership-source-review.md)의 실제 두 Map 구조를 구별한다. 오픽의 부분 제외/완료 복원·두Undo·추가 원본 수용은 해당 조합이 검증됐다. ALLBLANC의 단일 항목 전체 제외 거절은 정책 확정이나 성공적인 제외 기능이 아니다. 현재1184모델/strict311과 기록·측정 오류 구분을 유지한다.
2. 다음은 기존 개발2의 병합/분할·상세 속성·원문/틀·저장 이력과 실행/공개를 원래 기능·control별로 대조하고 미연결/미검증을 종결한다. 원래 화면에서 가능했던 동작을 새 문서의 간단한 편집이나 가져오기만으로 대체하지 않는다.
3. 실제 Map의 새 항목 추가/삭제·명시 선택과 다른 반복/과거기록 조합은 별도 잔여로 유지한다. 이미 완료한 오픽 조합을 다시 반복해 첫/둘째 전체 평가로 세지 않는다. 전체10상황의 구조가 다른 평가·발견 결함·수정/재검증을 함께 종결한다.
4. 긴 비교·모바일 스크롤·누적 자료의3~4초와 Map4506ms를 별도 개선하고 캡처 보고를 마무리한다. 실기기/외부계정/관찰사용자, 전체 제외·새 실행·백업·재공개 정책, 발행/배포·보고서 렌더 차단은 별도 경계다. 전체 목표는 active다.

## 이전 실행 순서 — pO7 원본Undo 후 추가 진행 검증 뒤

1. [복원 뒤 실행 원장](map-restored-progress-review.md)의 실제 실패→guard 수정→첫19/같은입력재개10/기록11대조와1182/strict311/build를 유지한다. 해당 일반 진행 추가 저장은 해결이며 같은 성공을 반복해 전체 상황 완료로 세지 않는다.
2. 실제 Map의 포함/제외·새 원본·개인 계획·지난 기록을 함께 사용한 다른 긴 조합을 확인한다. 제외/재포함의 의미, 새 항목 명시 선택, source와 개인 기록의 각 Undo를 원래 owner에서 검증한다. 전체 child 제외 정책을 확정하거나 가짜TXT·쉬운 합성 자료로 바꾸지 않는다.
3. 기존 개발2의 병합/분할·상세 속성·원문/틀·저장 이력과 실행/공개 연결을 원래 기능별로 대조하고 남은 동등성을 종결한다. 첫 평가/다른 구조 평가·결함 수정/재검증을 전체10상황과 연결한다.
4. 누적 자료의 긴 비교/모바일 이중 스크롤·Map4506ms를 개선하고 최종 캡처 보고를 완성한다. 단일 일반 진행217ms는 Map 저장과 다른 작업이다. 실기기·외부계정·관찰사용자·미확정 정책·발행/배포·보고서 렌더 차단은 별도 경계로 유지한다. 전체 목표는 active다.

## 이전 실행 순서 — QGp Map 기준일·출력 복합 검증 뒤

1. [Map 기준일·출력 원장](map-anchor-output-review.md)의 같은 자료35확인·실제 파일/소스15대조와 최종1180/strict311/build를 현재 근거로 유지한다. 이전 문서의 공통/하위 기준일·출력 전부 미연결 판정을 재사용하지 않는다. 전체npm 기존 출처기한1FAIL과4506ms 응답을 통과로 바꾸지 않는다.
2. 보존된 과거 기록 Map 프로필에서 원본 Undo 후 일반 항목의 추가 진행 저장을 확인한다. 화면 접근 허용과 실제 writer guard가 어긋나면 해당 owner의 실제 근거를 확인해 좁게 보완한다. 품질 보류·원본 검토·손상/외부 tuple guard를 전반적으로 해제하지 않는다.
3. 실제 Map의 포함/제외·새 원본·개인 계획과 지난 기록을 연속 사용한다. 전체 child 제외 정책을 임의 확정하거나 쉬운 합성 자료/가짜 TXT로 대체하지 않는다. 기존 제작의 병합/분할·상세 속성·원문/틀·저장 이력과 실행/공개 연결을 원래 요구별로 종결한다.
4. 전체10상황의 첫/다른 구조 평가와 두 개선 루프, 누적 자료의 응답·긴 화면 부담을 완료 조건에 맞게 확인하고 캡처 보고서를 갱신한다. 실기기·외부계정·관찰사용자·새 실행/전체 백업/재공개 정책·발행/배포와 보고서 렌더 차단은 별도 경계다. 전체 목표를 축소하지 않는다.

## 이전 실행 순서 — dFR 과거 Map 기록의 연결·원본 Undo 검증 뒤

1. [현재 판정](current-validation.md)의 dFR1177/strict311/build와 [같은 과거 기록19/22/8/15확인](map-existing-records-review.md)을 현재 근거로 사용한다. 검토 진입 차단은 수정됐고, 이전 진행 표시 snapshot 실패는 편집기 코드 변경 없이 실제 대상/스크롤/펼침/재개 상태를 맞춰 종결했다. 과거 aak30의 초점 보완과 dFR의 이번 경로를 전체 상황 성공으로 합산하지 않는다.
2. 다음은 실제 Map의 공통/하위 기준일·포함/제외·실제 출력·새 원본과 개인 반복 계획/지난 기록의 연속 조합이다. 원본 Undo 이후 일반 기록의 추가 편집 가능 범위도 기존 writer guard와 화면 접근이 일치하는지 확인한다. 다른 구조를 쉬운 합성 예시나 TXT로 바꾸지 않으며 현재 두 Map 프로필의 기록을 초기화하지 않는다.
3. 기존 제작 세부 요구와 전체10상황의 첫/다른 구조 평가·두 개선 루프를 요구별로 종결한다. 실제 결함을 수정·재검사하고3~4초 응답과 긴 화면의 실행 접근 부담을 별도 평가한다.
4. 최신 코드/실행판/보고서의 요구 상태를 함께 갱신한다. 실기기·외부계정·관찰사용자·새 실행/전체 백업/재공개 정책·발행/배포와 보고서 렌더 차단은 별도 경계다. 전체 목표는 진행 중이다.

## 이전 실행 순서 — 2026-09-13 m5h 세 경로·채택 오류 방지 검증 뒤

1. [세 경로의 원본 대응과 결과](three-journeys-and-review-affordance.md)를 기준으로 S01/S05/S08의 이번 공통 회귀는 보존한다. 신규1140/strict304/build와 같은 제안의 수정 전후25확인·저장 bytes 대조를 완료했다. npm 기존 기한1FAIL·audit기존5FAIL·3~4초 응답을 통과로 바꾸지 않는다.
2. [실제 Map의 반복 연결 누락](structured-map-recurrence-gap.md)을 먼저 처리한다.04:51의 실제 원본 adapter·D1 expander 재사용·17표적·strict306/0은 끝났고 기존 통합 실행 context0은 남는다. 다음은 기록을 보존하는 명시 인계→별도 structured context/series identity 검증→통합 reader/기간/완료/개인 계획/출력→같은 기록·원본 갱신·Undo/reload→전체 검사다. ALLBLANC 화·목에서 시작일 수요일을 첫 회차로 추가하지 않고 개인 요일 근거를 보존한다. `program-map-plan-next`의 진짜2-child OPIc와 `program-common-anchor`의 명시 합성 그룹은 이 누락을 검증한 근거가 아니다. 제목의 '반복'을 규칙으로 추정하거나 채워진 프로필을 초기화하지 않는다.
3. 기존 제작의 세부 요구와 전체10상황을 첫 평가/다른 구조의 평가/결함 수정/실제 재확인으로 종결한다. 원본 구조에 맞지 않는 기능을 억지로 넣거나 검사를 위해 의미를 바꾸지 않는다. 아직 미검증인 결과는 이전 성공과 구분한다.
4. 캡처 보고서의 최신 대응·구현·검증·별도 정책 경계를 갱신한다. 보고서 렌더 정책 차단은 우회하지 않는다. 실제 기기/관찰 사용자·외부 계정과 새 실행/전체 백업/재공개 정책, Git 발행·배포는 별도다. 전체 목표는 진행 중이다.

## 이전 실행 순서 — 2026-09-13 Mgb 공통 저장 검증 뒤

1. 이번 중간 근거를 [현재 판정](current-validation.md)으로 고정한다. 관련52/strict304·신규1137·승인201/공개19·build와 동일 초안 전후10/10·S06 연속19·원문95·query12/이동23/손상Undo3·실제 보류Map3/18을 실제 실행했다. 기존 출처 기한1FAIL·의존성5FAIL과 남는3~4초 대기를 통과로 바꾸지 않는다.
2. 다음은 S01 독립 문서·S05 발견/부분 사본·S08 옛 판본 기여/최신판 비교의 남은 공통 저장 회귀다. [상황별 원장](journey-closeout.md)의 첫 평가/다른 상황/실패 수정 근거를 현재 owner·실행판과 연결한다. 원문95가 이 세 상황이나 기존 제작 전체 기능을 대신하지 않는다.
3. 실제 Map·반복·개인 기록·원본 갱신을 함께 쓴 잔여를 확인하고 결함을 수정한다. 이미 수행한 정상 경로 반복을 전체 개선 루프로 세지 않는다. 성능을 더 바꾸게 되면 같은 누적 자료로 전후 비교하고 입력/저장/Undo와 관련 상황만 다시 검증한다. 이력 축소·검증 생략·대기 연장은 하지 않는다.
4. 전체10상황의 요구별 종료와 최소 두 전체 개선 루프가 증명되면 캡처 보고서를 최종 갱신한다. 현재 HTML은 Mgb의 실제2캡처와 날짜를 밝힌 이전2캡처, 정적48검사·문서검사 PASS이며 렌더는 정책 차단으로 미실행이다. 실기기·외부계정·관찰사용자·새 실행/전체 백업/재공개 정책과 Git 발행·배포의 별도 경계는 유지한다.

## 이전 실행 순서 — 2026-09-13 V7 검증 뒤

1. 완료된 중간 근거는 [현재 판정](current-validation.md)으로 고정한다. V7 신규1130·타입303/0·승인201·공개19·build, 원래 실패 답글23/질문전체18, 원문 연속95·query12/이동23·실제 보류Map3/18을 이미 실행했다. 반복 실행만으로 진척을 대신하지 않는다. npm 기존 기한1FAIL과 이전 의존성5건은 별도 유지한다.
2. P08의 실제 누적 사용 응답성을 개선한다. 보존된 `question-full-v7.state.json`의 원문·공개·개인 이력은 유지하고, 저장6737ms·답글 미리보기7863ms의 검증/직렬화/입력 잠금 비용을 먼저 분리한다. 더 쉬운 fixture·이력 축소·검증 생략·대기 연장으로 통과시키지 않는다. 개선 뒤 같은 입력과 이전 실패/취소/재시도에서 효과와 보호 경계를 비교한다.
3. S01~S10의 첫 평가/다른 상황/수정/재확인 근거에서 아직 최신 공통 변경을 거치지 않은 조합을 요구별로 종결한다. 기존 제작 전체 동등성·반복/Map·개인 문서의 차이를 좁은 정상 시나리오로 대신하지 않는다. 부분 캐시 개선은 두 차례 전체 개선 루프가 아니다.
4. 캡처 중심 보고서의 현재 결과·남은 결함을 함께 갱신한다. 이번 보고서는48개 정적 검사·문서검사 PASS, 실제 렌더는 URL 정책 차단으로 미실행이다. 다른 주소·브라우저로 우회하지 않는다. 새 실행·전체 백업·재공개 정책, 실기기/외부계정/관찰사용자 및 발행은 별도 경계를 유지한다.

## 이전 실행 순서 — 2026-09-13 01:27 UTC

1. 실제 source/private 연속 상황의 하위 체크·CRLF identity·저장 용량 결함은 A93의 같은 실제 fixture 90확인으로 재검증했다. 이를 전체10상황 완료로 확대하지 않는다.
2. 새로 확인한 선택 TXT의 체크 중복·날짜 중복과 정확히 같은 출처 속성 중복을 수정했다. 원문 전체 TXT/개인 메모/미선택 내용/공개 serializer는 보존한다. 표적36/36·strict303/0 뒤 소스를 동결해 신규 전체→npm 회귀→build 순서로 확인한다.
3. 새 빌드에서 동일 원문→개인 기록 보존 수용→실제5다운로드→선택 공개→reload를 다시 실행한다. 출력 검사는 중복 없음까지 추가하며 실제 bytes/선택/privacy 검사를 제거하지 않는다. gate·이동·실제 품질 보류 Map은 별도 범위로 이어 확인한다.
4. 최신 판정과 오래된 요구 표의 상태를 구별해 S01~S10의 잔여 검사를 확정한다. 첫 평가/다른 구조의 두 번째 평가와 각 수정·재확인 근거를 연결하고 캡처 중심 보고를 갱신한다. 전체10상황·두 개선 루프가 증명되기 전 목표는 진행 중이다.

## 이전 결함 처리 순서 — 2026-09-12 23:50 UTC

1. 실제 D2 하위 체크 포함 원문의 stage 실패를 기존 fixture로 고정한다. 원 vendor의 지원 밖 의미 guard는 유지한다.
2. Program 전용 source-stage v2와 하위 확인 목록의 원본/작업/후보 비교를 설계·구현한다. 목록 단위의 명시 유지/수용, genuine source rows, 기존 snapshot과 journal 재생/Undo/CAS를 검사한다. 반복·없음 유지와 혼합한 경우도 포함한다.
3. 동일한 연속 상황에서 원본 수용→명시 저장→기존 개인 기록을 보존한 선택 수용→실제 TXT/CSV/ICS→선택 공개를 실제 실행한다. 하위 체크를 fixture에서 제거하거나 다른 초안으로 바꾸어 통과시키지 않는다.
4. 최신판의 이동/gate/진짜 품질 보류 회귀, 전체 검사/build/보호 manifest와 전체10상황·두 개선 루프 종료 조건을 확인하고 캡처 보고를 갱신한다. 이 목록의 일부 통과는 전체 목표 완료가 아니다.
