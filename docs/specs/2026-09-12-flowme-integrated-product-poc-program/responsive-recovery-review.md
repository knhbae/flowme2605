# 작성 공간과 공개 초안 복구 — 실제 개선과 한계

2026-09-13. 실행판 `I8U9kyFwGAs2ojVITan8B`. 전체 목표의 중간 개선이며 전체 UX 완료가 아니다.

## 원래 요구와 변경

승인 정본 P01/P06/P08 및 S01/S07/S09의 본문 중심·복구·모바일 조작 요구를 적용했다. 기존 [여행 캡처 평가](creator-mobile-travel-review.md)에서 확인한 실제 문제를 기준으로 삼았다.

| 문제 | 이번 변경 | 유지한 것 |
| --- | --- | --- |
| 375px 공개 제목의 마지막 글자만 다음 줄로 떨어짐 | 제목의 한글 단어 단위 줄바꿈·균형 잡기, 좁을 때 닫기 버튼을 다음 줄로 배치 | 제목 전문, 닫기·초안 보관 기능 |
| 공개 미리보기보다 큰 하단 보조 버튼 영역 | 저장 상태는 바로 보이고 TXT·재저장·버리기는 `초안 보관·복구`에서 열기 | 세 복구 기능과 44px 이상 버튼, 오류 때 자동 열림 |
| 복구 성공 뒤 조작하던 버튼이 숨을 위험 | 오류 때만 disclosure를 열고 성공 때 자동으로 접지 않음 | 같은 입력·같은 저장기·초점의 연속성 |
| 상단에 반복되는 PoC 저장 안내와 문서 제목/작업의 과도한 높이 | 상시 안내 하나 유지, 비어 있는 live region은 공간을 차지하지 않음. 문서 제목과 작업의 반응형 행 배치 | 실제 저장 중·성공·실패 알림, 문서 작업 전체 |

새 데이터 저장기, 공개 필드, 운영 writer, 문서 편집기의 원문 계약은 바꾸지 않았다. Figma 라우팅은 기존 앱 코드 검증을 선택해 Figma 파일을 만들지 않았다. `ui-ux-pro-max`의 웹 제목 줄바꿈 검색은 적합했지만 Next.js 전용 검색/재검색은 부적합·0건이라 프레임워크 지침으로 사용하지 않았다. `flow-ux-review`의 중복 안내 제거와 보조 행동 접기를 적용했다. React 검토에서 기존 hooks 순서·writer·비동기 동작을 유지하고 새 복구 effect가 입력/초점을 변경하지 않음을 확인했다.

## 실제 브라우저

29개 확인 (로컬 전용 근거: `../../../output/playwright/integrated-program/compact-recovery-resume-2026-09-13T14-34-16-933Z.json`): 기존 여행 원문·50%·비공개 메모·공개 판본을 유지한 채 주간 운동 문서의 공개 초안을 열었다. 복구 키보드 열기, 실제 저장 포트의 quota 오류 1회, 이전 bytes/입력 보존, 같은 입력 재저장, 복구 TXT 실제 수신, 닫기·원래 여행 문서 복귀·reload를 확인했다. 공개하거나 개인 내용을 바꾼 검사가 아니다. 주간 운동의 미공개 초안은 그대로 보존했다.

- 개인 공간:375×812,390×844,844×390,1024×768,1194×834,1440×900에서 페이지 가로 넘침0.
- 공개 화면:위6크기와390×360에서 제목/하단 높이 확인. 하단은 접힌 상태180px 미만. 이 작은 viewport를 실제 소프트키보드로 표현하지 않는다.
- 375px 개인 편집 영역은 처음 화면에서 약261px 노출됐다. 이전 캡처의 약192px보다 늘었으나,844×390 상단 위치에서는 약31px만 보여 추가 스크롤이 필요했다. 전체 편집 사용성이 해결됐다는 증거가 아니다.
- 첫 7개 확인 후 중단 (로컬 전용 근거: `../../../output/playwright/integrated-program/compact-recovery-2026-09-13T14-33-08-285Z.json`)은 disclosure 내부 locator에 dialog를 중복 지정한 QA 오류다. 초안·기록을 초기화하지 않고 같은 자료로 재개했다.
- 원 기록의 공개 제목 metrics에는 DOM width/height가 viewport 필드명을 덮은 계측 문제가 있다. 7크기 판정은 고정된 실행 코드와 크기별 확인점으로 확인하며 해당 metrics 필드를 viewport 값으로 인용하지 않는다.

캡처 직접 확인: 개인375 (로컬 전용 근거: `../../../output/playwright/integrated-program/compact-private-375-1789310061529.png`), 개인1024 (로컬 전용 근거: `../../../output/playwright/integrated-program/compact-private-1024-1789310061705.png`), 공개375 (로컬 전용 근거: `../../../output/playwright/integrated-program/compact-publisher-375-1789310062263.png`), 오류·복구375 (로컬 전용 근거: `../../../output/playwright/integrated-program/compact-publisher-error-375-1789310063823.png`).

## 평가와 새 발견

기본 입력·복구는 해당 시나리오에서 사용 가능하다. 에이전트 평가에서 인지 부담3/5, 조작성3/5로 남겼다. 좁은 가로 화면·긴 문서의 이중 스크롤·많은 설정 행·장시간 사용의 응답성은 미해결이다. 캡처에서 반복 설정이 메모 선택으로만 나오는 별도 기능 차이를 발견했고 [반복 공개 구조 손실](recurring-publication-fidelity-gap.md)을 미충족으로 등록했다. 보기 좋게 접은 것을 그 기능의 해결로 세지 않는다.

## 검사와 경계

표적35/35(추가3), 전체138파일1237/1237·skip0·검사 중 소스 변경0, strict316/진단0, 최종 build PASS. 첫 build는 컴파일 종료0이어도 도중2파일 변경 때문에 유효 근거에서 제외하고 변경0 재실행을 사용했다. npm2031중2030PASS/기존 출처기한1FAIL, 승인201·공개19 PASS. 정확한 실행 파일은 [현재 판정](current-validation.md)에 연결한다.

이 프로필의 운영 보호키는0개다. 허용 밖 쓰기·page/console 오류0은 확인했지만 채워진 운영 데이터 불변을 대신하지 않는다. [Map 검사](map-catalog-change-review.md)의 별도3키, 소스 보호4781/예상 밖0, vendor26파일819423bytes·writer0, v11 integrity는 별도 근거다. 기록·현재 소스7대조 (로컬 전용 근거: `../../../output/integrated-product-poc/recovery-map-crosscheck-2026-09-13T14-43-10-965Z.json`)는 새 브라우저 검사가 아니다. 실제 복구 TXT2251bytes·SHA256도 여기에 기록했다.

실기기·OS IME·보조기술·외부 계정 미실행, 관찰 사용자0명. commit/push/PR/merge/Preview/Production/외부 게시 미실행. 보고서 HTML은 URL 보안 정책 차단으로 렌더하지 않으며 우회하지 않는다.
