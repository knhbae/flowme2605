# M7-2 · 001 대리 조작 시험 결과

## 9/24 후속 — 하위 화면 미실행 안내

실계정 복원 범위 답변을 기다리는 동안 D2의 제작·커뮤니티·자료실 잔여를 확인했다. 요청이 겹쳐 거절된 경우 입력이나 선택은 보존됐지만, 하위 화면에 저장 중이라는 안내가 계속 남았다. **실제 처리 중 상태와 이미 거절된 요청의 결과를 분리했다.**

- 상위 Alpha 화면은 실제 controller 상태에 따라 일시 대기 안내만 표시하고 처리 종료 때 해제한다. 이후 발생한 다른 실패 안내는 지우지 않는다.
- 하위 화면의 공통 결과는 “다른 요청과 겹쳐 이번 요청은 실행하지 않았습니다. 필요한 변경이 남아 있으면 다시 시도해 주세요.”로 바꿨다. 앞선 요청이 같은 내용을 저장했을 가능성도 있으므로 데이터 전체가 미저장이라고 단정하지 않는다.
- 제작 입력·참여 초안·개인 Flow 비교·자료실3경로는 실패 시 baseline/선택을 유지하고, 성공한 재시도에서만 저장 결과를 반영한다. 자동 재시도 정책이나 writer·schema·계정 범위는 변경하지 않았다.

`flow-copy-editor`의 기준에 따라 실제로 진행 중인 상태와 사용자에게 재시도가 필요한 결과를 구분했고, 기존 미저장 경고를 무조건 삭제하지 않았다. React 검토에서는 최신 상태를 받는 안내 갱신과 기존 클라이언트 경계를 유지했다. 독립 읽기 전용 검토에서도 새 P1/P2를 발견하지 못했다.

| 검사 | 이번 결과 |
| --- | --- |
| 표적 검사 | **84/84**, 신규13개. 상위·개인공간57개와 자료실27개를 합한 실행 수이며 전체 통합 검사에 다시 합산하지 않음 |
| 신규 handler 검사 | Community 실제 save·Creator 실제 flushDraft·CopyInspector 실제 save에 busy/conflict/checking-result를 각각 반환시켜 입력/기준값 보존·false 결과·명시 재시도 성공 확인 |
| 자료실 검사 | 전체 가져오기·제작 콘텐츠 가져오기·제작 사본 만들기에서 busy 거절 후 미리보기/선택·원본 유지, 자동 재시도0, 명시 재시도 성공 |
| 기본 테스트 | `npm test` **2255/2255**, 실패/skip/cancel0, 수집 소스 변경0. 로컬 전용 `output/integrated-product-poc/npm-test-2026-09-24T01-53-15-131Z.json` |
| 타입 | 통합530진입점/599소스 진단0·검사기10/10. 신규 브라우저 도구1진입점과 참조 소스도 진단0 |
| production build | 종료0·수집 소스 변경0. 로컬 전용 `output/integrated-product-poc/build-2026-09-24T01-53-04-555Z.json`. 새 빌드로 로컬3104 재시작 |
| 브라우저 새 시나리오 | **23/23**. 서버 읽기 응답을 보류한 채 커뮤니티 초안 입력→저장 요청0·입력 보존→읽기 종료 후 상위 대기만 해제→명시 재시도1회 저장→reload 초안 목록 확인 |
| 브라우저 기존 회귀 | **30/30**, 편집 거절/나눈 편집/키보드 중복 요청/완료/다시 열기/reload 검사 재실행 |
| 전체 통합 검사 | **247파일·2476/2476**, 실패/skip/cancel0, 수집 소스 변경0. worker2·각 heap512MiB, 11분34초. 로컬 전용 `output/integrated-product-poc/new-tests-2026-09-24T01-55-01-752Z.json` |
| 문서·변경 범위 | 문서 검사4/4·링크/skill 동기 검사 통과. 종료 절차의 scoped 변경 점검·공백 검사 통과, staged0 |

브라우저는 현재 production build의 실제 UI와 합성 Auth/API를 사용했다. 새 검사에서 실제 저장 함수의 순수 transition을 사용해 **메모리상의 비공개 참여 초안만1회** 갱신했다. 기존 회귀의4회 메모리 저장과 실제 DEV 저장을 혼동하지 않는다. 두 검사 모두 실제 계정 로그인/원격 writer 전달0, reload 전후 허용 prefix 밖 저장 호출0·운영 sentinel 문자열 정확 일치·콘솔/page error0이다. 실제 운영 자료 전체의 최신 hash를 다시 읽었다는 의미는 아니다.

로컬 전용 브라우저 근거:

- `output/playwright/alpha-m72-busy-result/2026-09-24T01-58-08-023Z/result.json`
- `output/playwright/alpha-m72-edit-feedback/2026-09-24T01-57-21-259Z/result.json`

새 검사 화면5장을 직접 검토했다. 390×844·375×812는 안내와 재시도 버튼이 줄바꿈되어 보이고, 844×390은 세로 스크롤 후 버튼이 가려지지 않았다. 1024×768·1440×900도 가로 넘침/재시도 버튼 가림0이다. 이 결과는 해당 안내·재시도 상태의 평가이며 다른 모든 제작·커뮤니티 화면의 완성도나 실제 휴대폰 평가가 아니다.

초기 실패도 보존했다. 자료실 신규3검사는 공통 문구 반영 전 새 기대 문구에서 실패했고 최종27/27로 통과했다. 신규 브라우저 도구는 fixture의 넓은 문자열 타입2건을 명시 타입/런타임 검증으로 수정했다. 브라우저 최초2회는 `본문`/실제 `내용` label 불일치, reload 뒤 접힌 초안 목록을 펼치지 않은 검사 절차 때문에 실패했다. 제품 동작이나 네트워크 경계를 우회하지 않고 locator/목록 펼치기를 고쳐 최종23/23을 확인했다.

이번 런타임 변경은 `ui-contract.ts`와 `AlphaWorkspace.tsx`의 안내 분리뿐이다. `AlphaWorkspace.test.tsx`·자료실 테스트3개·새 `ProgramMutationBusy.test.ts`·새 [브라우저 검사 도구](../../../scripts/alpha/m72-busy-result-browser.ts)와 원장을 함께 수정했다. 기존 dirty 변경은 보존했다. 실제 사용자 T04 복원·실기기·일상 사용·독립 매체 사본은 여전히 미실행이다. 관찰 사용자0, commit/push/PR/merge/Preview/Production 모두 미실행, M7-2 전체 목표는 유지한다.

## 9/24 후속 — 복원 전 현재 상태 대조

D1·D2 수정 뒤에는 T04 명시 복원의 준비를 우선했다. **현재001은 r441이며 이전 대리 시험 직후와 자료가 같다. 복원 적용은 아직0건이다.** 기존 실제 계정 복원의 대상/차이 확인 조건을 유지하며, 사용자에게 시험용 메모 한 줄 추가→보존한 백업으로 복원하는 범위를 질문했다. 이전 백업 위치나 이미 승인한 작업을 다시 묻는 것은 아니다.

이번 확인은 2026-09-24 01:41–01:43 UTC에 실행했다. 새 세션으로001의 백업을 읽고 서버 동일 상태 미리보기를 확인한 뒤 그 세션만 로그아웃했다. 새 파일은 Git 밖 기존 `D:\FlowMe-Backups\trial-user`에 덮어쓰기 없이 보관했다. 파일명 `2026-09-24T01-41-50-817Z-2241934e-before-trial.json`, 107,910 bytes, SHA-256 `cf525395d0962eb971785c77088ce3759ec48a522a7c6f3b80a3f5546aa2fa12`다. 다른 물리 매체의 사본이나 암호화 백업으로 세지 않는다.

| 확인 항목 | 이번 결과 |
| --- | --- |
| 이전 시험 직후 백업과 새 백업 | account·references·operations·importArchives·files의 정규화 JSON 각각 정확 일치. 백업 생성 시각/봉투는 다르므로 파일 전체 hash 일치를 주장하지 않음 |
| 개인공간 | 문서2개 모두 `[검증용 M7-2 0924]` 표시, r441. 개인공간 SHA-256 `9ba2f6915f18b0e05928530efba56f5d4fba9cb70d88c381a7eb7d1e2bbe1dc3` 일치 |
| 기록·사진 |001 기록441건 동일, 백업 첨부0. 실제 사진 복원 시험으로 세지 않음 |
| 서버 미리보기 | `same=true`, `canApply=false`, r441. 적용 요청0 |
| DEV 보호 범위 재대조 |3계정·전체 기록542건·보호2계정/기록101건·공유 자료·사진8개·보존 사진/이관 보관소·validator 상태가 직전 종료 근거와 각각 일치. 인증 세션까지 불변이라는 뜻은 아님 |
| 표적 자동 검사 | 백업 취득·오프라인 검사·파일 변경 감지·서버 보존 handler **69/69**, 실패/skip/cancel0. 잘못된 계정·손상·변경된 판본 거절, 본인 개인공간만 복원하고 과거 서버 기록을 재실행하지 않는 경계 포함. 합성/임시 파일 검사이며 실계정 복원과 구분 |

로컬 전용 근거: `output/alpha-m72-restore-preflight-20260924.json`. 비교용 첫 명령은 TypeScript 모듈의 ESM/CJS 읽기 방식 불일치로 자료를 읽기 전에 실패했고, 같은 비교를 CJS 방식으로 실행해 통과했다. 런타임·저장 계약 수정은 없다. `npm test`·build·브라우저 전체는 이번 준비 단계에서 재실행하지 않았으며 아래 D1·D2 실행 기록을 그대로 유지한다.

승인 시 다음 순서: 현재 상태/동시 변경 재확인 → `[검증용 M7-2 0924] 일정·메모`에 식별 가능한 시험 문장 한 줄 추가 → 변경 후 상태도 새 백업 → 복원 미리보기와 차이가 그 문서의 허용 변경뿐인지 확인 → 명시 복원 → 원문/줄 ID/개인 상태 대조·새 로그인·보호 범위 재대조. 다른 자료 변경이나 예상치 못한 차이가 있으면 적용하지 않는다. r427의 빈 개인공간으로 되돌리거나 문서2개를 삭제하는 시험이 아니다.

새 앱 자료 쓰기0·실계정 복원0. 실제 기기 미실행, 관찰 사용자0, commit/push/PR/merge/Preview/Production 모두 미실행. M7-2는 계속 진행 중이며 아래는 앞선 수정·시험 기록이다.

## 9/24 후속 — D1·D2 수정과 격리 재검사

아래 최초 시험에서 발견한 두 안내 문제를 M7-2의 결함 수정 범위로 처리했다. **편집 연결 보호는 유지하고 거절 원인을 구분했으며, 개인공간의 저장 중 연속 조작 안내를 수정했다.** 전체 M7-2나 모든 제작·커뮤니티 화면을 완료한 판정은 아니다. 최초 실제001 시험과 이번 가짜 계정 재검사의 근거를 합산하지 않는다.

| 문제 | 수정 내용 | 이번 재검사 |
| --- | --- | --- |
| D1: 편집 거절 원인 오안내 | 기존 `editText`의 수용/거절 조건은 유지. 별도 일시 결과로 항목 연결 모호·입력 형식·그 밖의 보호 거절을 구분. 연결이 모호하면 제목 수정과 줄 이동을 나누거나 제목을 한 줄씩 저장하도록 안내 | 거절 원문 보존·추가 저장0. 제목 수정→내어쓰기 각각1회 저장, 모든 줄 ID 유지. 형식 오류·보호 문서·저장 실패·입력 복구 회귀 검사 |
| D2: 저장 중 조작을 충돌로 안내 | 진행 중 요청은 `busy`, 미해결 결과/초안은 기존 `unresolved`로 분리. 상위 안내는 실제 요청 종료 때 해제. 개인공간 하위 화면에 같은 안내가 남지 않도록 중복 표시 제거 | 응답을 보류한 상태에서 완료 버튼 클릭→Enter 재클릭. 저장 요청1건만 발생. 응답 후 안내 제거→다시 열기 성공→reload 원문 일치. 실패·pending·conflict 입력 보관 경계 유지 |

브라우저는 **현재 production build의 실제 `/alpha` UI + 가짜 Auth 응답 + 메모리 서버**를 사용했다. 실제001/002 로그인·원격 DB 요청 전달·원격 저장·전체 복원은0이다. 테스트 서버의 저장4건은 제목 수정·내어쓰기·완료·다시 열기이며 실계정 판본 변경이 아니다. reload 전후 모두 허용 prefix 밖 저장 호출0, 운영용 sentinel 문자열 정확 일치, 콘솔/page error0을 확인했다. 실제 운영 DB 전체를 다시 대조했다는 의미는 아니다.

검증 결과:

- 표적 검사: 편집기20/20, 저장 제어·Alpha 화면·개인공간 경로66/66. 신규 회귀11개. 아래 전체 통합 검사와 중복 합산하지 않는다.
- `npm test`: **2255/2255**, 실패/skip/cancel0. 로컬 전용 `output/integrated-product-poc/npm-test-2026-09-24T01-20-16-389Z.json`.
- 타입: 통합529진입점·598소스 진단0, 검사기10/10. 브라우저 도구1진입점/참조 타입 진단0.
- production build: 종료0, 검사 중 소스 변경0. 로컬 전용 `output/integrated-product-poc/build-2026-09-24T01-20-42-467Z.json`. 새 빌드로 로컬3104 서버 재시작.
- 전체 통합 검사: **246파일·2463/2463**, 실패/skip/cancel0, 검사 중 소스 변경0. 로컬 전용 `output/integrated-product-poc/new-tests-2026-09-24T01-20-05-645Z.json`. 병렬 worker2·각 heap512MiB로 실행했으며 검사 항목은 제외하지 않았다.
- 문서 검사: 종료0, 검사기4/4·링크/문서/skill 동기 검사 통과. scoped diff 공백 검사 통과, staged0.
- 브라우저: **30/30 확인 항목**, 격리 계정1·브라우저 컨텍스트1·화면6장 직접 검토. 로컬 전용 `output/playwright/alpha-m72-edit-feedback/2026-09-24T01-26-24-276Z/result.json`.

| 화면 크기 | 이번 확인 범위와 결과 |
| --- | --- |
|390×844|편집 거절 안내·원문 보존·복구 버튼 표시, 가로 넘침0|
|375×812|탭과 버튼 줄바꿈, 안내 상자 전체 표시, 가로 넘침0|
|844×390|세로 스크롤 후 안내·복구 버튼 전체 표시, 가로 넘침0|
|1024×768|문서 목록과 편집 영역, 안내 잘림·가로 넘침0|
|1440×900|편집 안내 및 저장 대기·키보드 재클릭·안내 해제 확인|

초기 표적 검사1회는 잘못된 날짜 값을 기존 모델이 차단한다고 가정해1개 실패했다. 현재 계약은 원문을 보존하는 비차단 경고이므로 제품 정책을 바꾸지 않고 테스트 가정을 고쳐 최종20/20을 확인했다. 브라우저 최초2회는 도구 실패였다: 같은 이름의 문서 버튼2개로 locator 모호, 그 다음에는 이미 가짜 응답으로 처리한 계정 읽기 GET을 결과 검사 허용 목록에서 누락했다. 탐색 범위와 검사 목록만 바로잡아 최종30/30을 확인했으며 실패 원장은 보존했다. 두 실패에서도 원격 요청 전달은 없었다.

이번 변경 파일은 `ProgramTextEditor.tsx/test`, `text-workspace.ts`, `vendor/text-model.cjs`, `alpha-sync/controller.ts/test`, `AlphaWorkspace.tsx/test`, `ProgramSpace.tsx`, 새 `ProgramSpace.busy.test.ts`, 공통 `ui-contract.ts`의 busy 문구, 새 [격리 브라우저 검사 도구](../../../scripts/alpha/m72-edit-feedback-browser.ts), 이 원장·상위 시험 원장·STATUS다. 기존 dirty 파일에서는 해당 부분만 추가 수정했다. Next/React 검토에서 새 서버/클라이언트 경계를 만들지 않았고, 안내 해제는 최신 상태를 받는 갱신 방식으로 기존 실패 안내를 보존했다. 문구 검토는 실제 거절 원인·사용자가 취할 다음 행동에 한정했다.

잔여: D1의 모호한 여러 줄 편집은 여전히 거절된다. 이를 안전하게 더 수용하는 편집 알고리즘 변경은 이번 범위가 아니다. D2의 제작·커뮤니티·자료실 일부 하위 화면은 자체 대기 안내가 남을 가능성을 코드에서 확인했으며, 개인공간 수정 완료를 그 화면까지 확장하지 않는다. 다음 기술 검증은 해당 화면의 입력 보존·중복 요청·안내 종료를 함께 재현하는 것이다. 실제 일상 사용·실기기·독립 매체 사본·선택 자료 명시 복원·큰 자료 지연은 기존 잔여로 유지한다.

Android Chrome/iOS Safari 실제 기기: 미실행. 관찰 사용자:0명. commit/push/PR/merge/Preview/Production: 모두 미실행. 아래는 **수정 이전 최초 대리 시험**의 기록이다.

## 최초 시험 기록

검사일: 2026-09-24. 사용자 요청: “너가 테스트해줘봐..”. 개발계 http://localhost:3104/alpha 에서001의 검증용 자료만 직접 조작했다. 제품 코드·서버 계약은 변경하지 않았다.

## 판정

**소량 자료의 기본 저장·이동·완료·Undo·재접속·백업 흐름은 확인했다. 일상 실사용 검증이나 M7-2 전체 완료는 아니다.** 편집 거절의 원인 안내와 저장 중 연속 조작 안내에서 개선할 부분2개를 찾았다. 실제 기기·계정 전체 복원은 이번에 실행하지 않았다.

001에 `[검증용 M7-2 0924]`로 표시한 문서2개·할 일3개·새 폴더1개를 남겼다. 사용자의 실제 일정으로 꾸민 내용은 없다. 확인용 자료를 임의 삭제하거나 계정을 초기화하지 않았다.002에는 새 로그인·쓰기를 하지 않았다.

## 시나리오 결과

| 시나리오 | 결과와 근거 |
| --- | --- |
| 시험 전 보존 |001 r427·문서0의 서명 백업 생성, 파일 재읽기 hash 일치, 서버 동일 상태 preview 성공. 기존 파일 덮어쓰기0 |
| 메모·할 일 작성 |검증용 문서와 메모, 할 일2개 저장. 빠른 입력으로 날짜 있는 할 일1개와 전용 문서 생성. 뒤에 문서 이름에도 검증용 표시 |
| 빈 문서 작성 예시 |접힌 예시를 열어 실제 예시 본문과 넣기 버튼 표시 확인. 예시 삽입 자체는 실행하지 않음 |
| 여러 줄 동시 수정 |부모 제목 변경과 자식 내어쓰기를 동시에 하면 저장 거절. 입력은 남고 저장 원문은 보존됨. 아래 D1. 순서대로 나눠 수정한 뒤에는 저장·기존 줄 ID 유지 확인 |
| 날짜 이동·Undo |빠른 일정09/24→09/25, 오늘에서 빠지고 주간에09/25로 표시. Undo 후09/24로 복귀. 동일한 task ID 유지 |
| 같은 날짜·Escape |같은 날짜 적용과, 변경할 날짜 입력 후 Escape 취소에서 r431 유지·추가 저장 요청0 |
| 완료·다시 열기 |오늘에서 완료, 월간의 체크 표시와 문서의 `[x]`를 확인. 키보드 Enter로 다시 열고 `[ ]`·미완료 복원 |
| 폴더 이동·Undo/Redo |빠른 할 일 **문서**를 시험 폴더로 이동, Undo로 미분류, Redo로 시험 폴더. 문서 위치와 할 일 소속은 다른 개념이며 이 시험을 Flow Item 폴더 상속 검증으로 확대하지 않음 |
| 저장 중 연속 조작 |Redo 저장 중 바로 다시 열기를 실행하자 쓰기는 차단됐지만 “해결하지 않은 변경…비교”로 안내. 저장 완료 후 재시도 성공. 아래 D2 |
| 새로고침·별도 로그인 |r441에서 두 문서·세 할 일·폴더·원문이 복원됨. 이후 새 브라우저 컨텍스트2개에서도 마지막 상태 projection 정확 일치 |
| 기간 보기 |오늘·주간·월간에 날짜 있는 항목 표시 확인. 날짜 미정에 두 항목 표시 확인. 모든 기간/필터 조합을 검사한 것은 아님 |
| 실제 UI 백업 |사진 포함 백업 버튼→다운로드→파일 재읽기→같은 파일 선택→서버 미리보기 성공. 현재/복원 후 문서2개, 동일 자료 안내, 복원 적용 버튼 없음. 모달 닫기 전후 전체 account 동일 |
| 보호 자료 |아래 별도 읽기 전용 DB 대조에서002·별도 QA·공유 자료·사진 불변. 운영계 접근/배포0 |

실제 실행된 저장 API는 **14건 모두 성공**,001 판본427→441, 전체 작업 이력528→542였다. 같은 위치·Escape·거절 입력·저장 중 중복 조작을 성공 저장으로 세지 않았다. 다른 사용자 자료를 덮는 전체 복원·삭제는0건이다.

## 화면별 평가

검사 범위는 **다섯 viewport의 오늘 목록과 날짜 대화상자**다. 각 크기에서 목록 항목 표시, 페이지 가로 넘침, 날짜 값, 적용 버튼의 실제 클릭 위치 가림, Escape로 닫기를 확인했다. 총10장 화면을 직접 검토했다.

| 크기 | 결과 | 제한/관찰 |
| --- | --- | --- |
|390×844|통과|문서 목록은 접기 방식, 오늘 항목까지 세로 스크롤. 날짜 창 하단은 내부 스크롤 |
|375×812|통과|보기 탭이 두 줄로 배치됨. 제목 줄바꿈, 가로 넘침 없음 |
|844×390|통과|낮은 높이에서 본문·대화상자 세로 스크롤 필요. 날짜 적용 버튼은 가려지지 않음 |
|1024×768|통과|좌측 문서 목록과 오늘 영역 표시, 날짜 창은 세로 스크롤 |
|1440×900|통과|문서 목록·오늘 영역·날짜 창 표시 정상 |

후속 읽기 전용 검사에서는 콘솔 오류0·page error0·요청 차단0이었다. 첫 조작에서는 검사 도구가 읽기 API `/auth/v1/settings`를 빠뜨려2회 차단하고 콘솔 오류2개를 유발했다. 해당 GET을 허용한 독립 세션에서 재검사했으며, 최초 오류 기록은 지우지 않았다. 첫 도구의 locator/모달 조작 실패3건도 제품 실패와 별도 기록했다. Android Chrome·iOS Safari 실기기 검사는 미실행이며 관찰 사용자0명이다.

## 남은 결함과 다음 작업

| ID | 발견한 문제 | 원인·다음 확인 |
| --- | --- | --- |
|D1 · P2|일반적인 여러 줄 편집이 거절될 때 날짜·진행률·들여쓰기 문제라고만 안내|text-model의 ambiguous gap 보호가 부모/자식 줄 identity를 확정하지 못함. 단일 제목 변경·내어쓰기·문자열 제거는 성공하며, 순차 적용도 기존 ID를 유지함. 보호를 제거하지 말고 구체적 원인·나눠 편집하는 방법을 안내하고 안전하게 수용할 수 있는 범위를 검토 |
|D2 · P2|저장 중 연속 조작을 충돌 해결이 필요한 상황처럼 안내|busy와 pending/draft가 unresolved를 공유함. 실제 저장 성공14건에409/실패 응답은 없었음. busy의 행동 비활성화 또는 별도 기다림 안내와 저장 후 메시지 해제를 검토 |

이번 요청은 대리 테스트이므로 위 두 제품 동작을 임의 변경하지 않았다. 즉시 테스트 사용을 위한 다음 기술 작업은 **D1·D2 수정과 같은 조작 재검사**다. 대규모 합성 QA를 늘려 실제 사용을 대신하거나 백업 폴더 선정을 새 차단 조건으로 만들지 않는다.

별도 잔여: 실제 일상 사용, 실제 Android/iOS, 다른 기기와의 실제 접속, 독립 매체 사본, 선택 자료의 명시 복원, 장기 안정성·큰 자료 지연, 남은 콘텐츠 연결. drag·길게 누르기·pointer cancel·실제 저장 오류 주입·기존 /my 및 운영 flow:* key 전후 검사는 이번 소량 시험에서 재실행하지 않았다. 기존 근거와 이번 결과를 합쳐 새로운 전체 통과로 표시하지 않는다.

## 기존 자료 불변 증거

각 행 전체를 JSON으로 만든 뒤 행별 hash를 정렬 집계해 전후 비교했다. 단순 개수 비교가 아니다. 아래는 DEV이며 운영계 조회/변경은 하지 않았다.

| 보호 범위 | 전후 개수 | 전후 동일 hash |
| --- | --- | --- |
|001 제외 계정(002와 별도 QA)|2|`ae4aa91584ef29d626d2da998ed54c94`|
|001 제외 작업 이력|101|`07e7781af7908f4218509e3eedc4a118`|
|001의 기존 r427 이하 이력|427|`47648be517c1b3a74d17b9a4aa7ef75a`|
|공유 상태|1|`330fe3319139ed53704135eec51d8a36`|
|공유 사진|8|`f52007799b665e289d1da539ca91a4b4`|
|반입 보관·보존 사진|각0|각 `d41d8cd98f00b204e9800998ecf8427e`|

001은 승인된 검증 자료 때문에 달라졌다. 이를 운영 데이터 전체 불변으로 표현하지 않는다. 기존 이력 hash의 첫 측정은 초기 문서 생성/저장 직후였고, 이후에는427개 전부 불변이었다. 다른 계정·공유 자료의 기준선은 첫 조작 전 측정했다.001의 기존 비텍스트 영역은 저장 상태 검사마다 시험 전 백업과 일치했다. 읽기 전용 후속에서는 화면 조작·백업 전후 **전체 account 정확 동등성**도 확인했다.

## 백업과 로컬 근거

두 파일은 기존 사용자 백업 폴더에 새로 만들었으며 Git 밖에 있다. 암호화되지 않은 개인 자료이므로 공개하지 않는다. 위치를 영구 제품 정책이나 독립 매체 확인으로 간주하지 않는다.

- 시험 전: `D:/FlowMe-Backups/trial-user/2026-09-24T00-42-21-299Z-26e3a27f-before-trial.json` ·104,598bytes·r427·문서0·이력427. 파일 SHA256 `42cb6df319cdcb1cc740aba32d63e217d13668b8b69ffc487e545ad86e94f4c6`.
- 시험 후 실제 UI 다운로드: `D:/FlowMe-Backups/trial-user/2026-09-24T00-59-58-357Z-after-delegated-trial.json` ·107,910bytes·r441·문서2·이력441. 파일 SHA256 `1090adc67b4905c0eb7d075c601e1c753b3a153cfc21d83c2ec0ca570bcc9323`.
- 오프라인 파일 검사는 형식/owner/hash 검사다. 서버 서명 검증은 별도 실제 preview 성공 근거이며, 전체 복원 적용 성공과는 구분한다.
- 조작 원장·화면: 로컬 전용 `output/playwright/alpha-m72-delegated-001/2026-09-24T00-50-42-980Z/`.
- 독립 로그인·다섯 viewport·실제 백업 UI: 로컬 전용 `output/playwright/alpha-m72-delegated-001/2026-09-24T00-59-09-808Z/`.
- 보호 자료 전후 대조: 로컬 전용 `output/alpha-m72-delegated-trial-protection-20260924.json`.

## 변경 파일·자동 검사·발행 상태

변경은 로컬 전용 조작 도구 `scripts/alpha/m72-delegated-trial-driver.ts`, 이 결과 문서, [상위 시험 원장](alpha-m7-2-personal-trial.md), [STATUS](../../STATUS.md)다. 전용 도구는 실제 계정에 고정되어 공개 Git 게시 후보에서 제외하며 파일을 삭제하지 않는다. 제품 소스·DB schema·설정·원래 작업 공간의 dirty 파일은 바꾸지 않았다. 도구는 최초 쓰기 전 r427 백업 일치 검사를 요구하므로 현재 상태에서 쓰기 모드 재실행은 거절된다. 후속 verify-only는 저장/복원 적용 요청을 차단한다. 브라우저 시작·결과 저장 실패에도 검사 lock을 해제하도록 도구의 정리 경계를 보완했다.

- npm test: 집계가 남는 최종 실행 **2255/2255**, 실패/skip/cancel0. 로컬 전용 `output/integrated-product-poc/npm-test-2026-09-24T01-02-37-796Z.json`. 앞서 직접 실행한 npm test도 종료0이었으나 중복 합산하지 않는다.
- 도구 타입 검사: 최종1진입점과 참조 타입 진단0. 첫 도구 시작의 CommonJS top-level await 오류는 main 함수로 감싸 해결했으며, 그 실패 시점에는 로그인/자료 쓰기가 없었다.
- 브라우저: 조작1세션+검증 전용2세션. 위 다섯 viewport 묶음5/5, 실 UI 백업/동일 상태 preview1회. 기능 전체의 자동 테스트 수나 관찰 사용자 수로 환산하지 않는다.
- 문서 검사: docs:check 종료0, 검사기 테스트4/4·문서/링크/skill 동기 검사 통과. 로컬 전용 `output/integrated-product-poc/docs-2026-09-24T01-08-39-839Z.json`.
- production build: 제품 소스 변경이 없어 이번에는 재실행하지 않았다. 이전 build 성공을 이번 실행으로 세지 않는다.
- 도구 최종 정리 경계 변경은 코드/타입 검토 대상이며 실패 주입 시험을 했다고 주장하지 않는다.

commit: 미실행. push: 미실행. PR: 미생성. merge: 미실행. Preview: 미배포. Production: 미배포. 관찰 사용자:0명.
