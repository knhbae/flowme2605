# 일반 항목의 설명·자료를 공개 초안에 연결

2026-09-14 · 실행판 `vJ949EZ2h-6vM6Q3uSi68` · P06/P07/P08의 해당 연결을 검증했다. 전체 목표는 진행 중이다.

## 원래 요구와 적용

| 요구 | 확인한 차이 | 현재 적용과 근거 |
| --- | --- | --- |
| 개발2의 실제 원문 설명·완료 조건·자료 보존 | 일반 공개 초안은 제목 외 필드가 비어 이사 설명과 결혼 설명/자료를 수동으로 다시 입력해야 했다 | raw/native 불변 수용 판본의 정확한 행을 읽어 제목·설명/자료·완료 조건·출처 중 선택한 필드만 초안에 저장 |
| v4.1의 개인 기록과 공개 경계 | 개인 본문을 통째로 가져오면 메모·날짜·완료가 섞일 수 있다 | 개인 객체를 후보로 쓰지 않는다. 실제 50% 기록·메모·원문/제작·다른 인물의 공간을 그대로 보존 |
| 개발1의 공개 판본 유지 | 이미 공개한 첫 판본을 수정해서는 안 된다 | 같은 공개 Flow/항목 ID와 미정 일정·하위 체크를 유지한 새 2판만 생성. 이전 모든 공개 판본 불변 |
| 실수와 저장 실패 복구 | 비교 선택이 바로 공개되거나 입력을 덮으면 안 된다 | 기본 미선택, 취소/Escape·같은 값은 쓰기0. 강제 저장 오류에서 선택/입력/저장 bytes 유지, 키보드 재시도와 초점 복귀 |

세부 판단은 [설계](ordinary-publication-source-design.md)와 [여섯 작성물의 소비 대조](creator-template-consumption-review.md)를 따른다. 새 운영 schema·자동 migration·외부 게시를 추가하지 않았다. 공개 일정/하위 체크 선택은 기존 경로를 유지했다.

## 실제 실행과 중단을 구별한 기록

1. 첫 실행 (로컬 전용 근거: `../../../output/playwright/integrated-program/ordinary-source-review-2026-09-14T07-14-09-928Z.json`)은 초안264를 저장한 뒤 화면 대기가 끝나 2확인에서 중단했다. 마지막 snapshot에는 실제 새 판본 공개 dialog가 있다. 당시 전체 회귀·npm·서버 프로세스는 이후 핸들과 실제 프로세스가 사라졌고, 회귀/npm은 종료 결과 파일이 없어 통과로 세지 않았다.
2. 기존 임시 브라우저가 종료돼 프로필도 없었다. 복원 준비 (로컬 전용 근거: `../../../output/playwright/integrated-program/ordinary-source-recovery-2026-09-14T08-19-00-173Z.json`)의 실제 저장264를 현재 store validator로 검사한 뒤 빈 전용 QA 프로필에 정확한 PoC key 하나만 복원했다. SHA256은 `c6613041351b1472b19c226a386937defbde36c0cf32e57e1b420211e104d163`이다. 이는 제품의 사용자 복원 기능이나 새 사용자 입력 시나리오가 아니다. 준비 스크립트의 결과 필드명/CLI 실행 환경 오류도 제품 결함과 구별한다.
3. 이사14확인 (로컬 전용 근거: `../../../output/playwright/integrated-program/ordinary-source-recovered-run-2026-09-14T08-19-17-878Z.json`)은264→266에서 실패/재시도·취소·Escape·같은 값·개인 보호·새2판까지 통과했다. 마지막 검사는 주 메뉴의 일반 `내 공간` 주소를 특정 문서 주소로 잘못 기대해 중단했다. 이어진 자동화에서도 전환 완료 전에 URL을 읽어 중단했지만 저장 변경은0이었다.
4. 결혼 및 복귀23확인 (로컬 전용 근거: `../../../output/playwright/integrated-program/ordinary-source-wedding-final-2026-09-14T08-21-24-973Z.json`)은266→269이다. 문서 선택과 URL 전환 완료를 기다린 뒤 정확한 이사 문서 복귀를 확인했고, 결혼의 설명/자료·오류/재시도·새2판·복귀/reload를 완료했다. 이사2판을 다시 만들거나 개인 기록을 초기화하지 않았다.
5. 원본/저장/소스20대조 (로컬 전용 근거: `../../../output/integrated-product-poc/ordinary-source-crosscheck-2026-09-14T08-24-54-205Z.json`)는 복원264→이사266→결혼269의 연속성, 전체 개인 공간의 공개 연결/초안 이외 값, 다른 인물, 기존 공개 판본과383개 현재 소스의 build/test 일치를 확인했다. 이 대조를 새 자동 테스트 수로 더하지 않는다.

## 자동 검사

| 실행 | 실제 결과 |
| --- | --- |
| 관련 원문/반복/공개 화면 |83/83 통과 |
| 통합 전체 (로컬 전용 근거: `../../../output/integrated-product-poc/new-tests-2026-09-14T08-21-45-506Z.json`) |156파일,1549실행/1549통과/실패0/skip0. 실행 중 소스 변경0 |
| strict TypeScript |353진입, 진단0 |
| production build (로컬 전용 근거: `../../../output/integrated-product-poc/build-2026-09-14T08-13-49-550Z.json`) |통과, 위 실행판과 일치 |
| npm test (로컬 전용 근거: `../../../output/integrated-product-poc/npm-test-2026-09-14T08-22-09-157Z.json`) |2031실행/2030통과/기존 출처 검토 기한 검사1실패/skip0 |
| 기존 승인 실행 (로컬 전용 근거: `../../../output/integrated-product-poc/approved-tests-2026-09-14T08-24-54-685Z.json`) |201/201 통과 |
| 기존 공개 화면 (로컬 전용 근거: `../../../output/integrated-product-poc/public-tests-2026-09-14T08-24-54-686Z.json`) |19/19 통과 |

새 모델23개와 기존 공개 화면 파일의 추가4개는 통합 전체1549에 포함된다. 첫 표적 실행의 QA 자료 URL 문법과 손상 identity 가정 오류를 바로잡았으며 운영 parser/validator를 완화하지 않았다. dependency audit은 이번에 재실행하지 않았다.

## 화면 평가

결혼 비교의390×844,375×812,844×390,1024×768,1440×900 모두 문서/dialog 가로 넘침0, 적용 버튼44px·hit test 통과다. page/console error0. 이사 경로는1024 검사이며 여섯 작성물 전체가 모든 크기를 통과했다고 확대하지 않는다.

- 375px 비교 (로컬 전용 근거: `../../../output/playwright/integrated-program/ordinary-source-review-375-1789374089659.png`): 직접 확인했다. 필드는 잘리지 않지만 동일한 제목과 빈값까지 비교해 실제 설명 선택이 아래로 밀린다. 같은 필드의 접기/차이 우선 표시가 후속 UX 개선 대상이다.
- 1024px 공개 미리보기 (로컬 전용 근거: `../../../output/playwright/integrated-program/ordinary-source-wedding-preview-1789374092672.png`): 직접 확인했다. 원래 설명·자료 URL·두 하위 체크가 보이고 공개 실행 날짜는 미정이다. 오류 복구 메뉴가 열린 상태라 화면이 길며 자료 URL은 설명 속 텍스트다.
- 초점 복귀는 저장 중 disabled가 풀린 뒤 수행한다. 원본 전체 검증은 렌더마다 반복하지 않고 비교 열기/적용 때 수행한다. 응답 성능 전체 검증을 마쳤다는 뜻은 아니다.

내부 UX 평가: 내용 보존4/5, 원문/개인 경계4/5, 인지 부담3/5, 조작3/5. 새 화면을 만들지 않고 기존 항목 편집 안에서 요청할 때 비교를 표시했다. Figma 미사용 Code-only이며 실제 사용자 평점이 아니다.

## 보호와 미실행

브라우저 기록의 허용 prefix 밖 setItem/removeItem/clear는0이다. 강제 실패는 각1회이며 committed bytes가 유지됐다. 복원용 setItem1회는 기능 검사와 별도다. 해당 QA 프로필의 운영 key가0개이므로 채워진 실제 운영 데이터 불변 증거로 과장하지 않는다. 파일 보호 결과는 현재 기준선 검사에 별도 기록한다.

실제 Android Chrome/iOS Safari·OS IME·보조기술·외부 import·관찰 사용자는 미실행이다. 보고서 HTML은 기존 URL 보안 정책 차단으로 이번에도 렌더 검증하지 않았고 다른 경로로 우회하지 않았다. 실제 앱 캡처와 HTML 정적 검사를 구별한다.

| 발행/관찰 항목 | 상태 |
| --- | --- |
| commit | 미실행 |
| push | 미실행 |
| PR | 미실행 |
| merge | 미실행 |
| Preview | 미실행 |
| Production | 미실행 |
| 실제 외부 게시 | 미실행 |
| 관찰 사용자 수 |0명 |

## 남은 전체 작업

다른 origin/이동된 원문 연결, 일반 일정의 시간대/출력과 모든 항목 조합을 이 필드 연결로 완료 처리하지 않는다. Map 실제 삭제·개인 수정 충돌/복잡한 전환·긴 화면과 누적 응답·같은 최종판 S01~S10 및 최소 두 전체 평가→개선 루프는 계속 남는다. 이번 비교/개선/재확인은 국소 개선이며 전체 루프 한 회로 세지 않는다.
