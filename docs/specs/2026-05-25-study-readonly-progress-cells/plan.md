# Plan

1. Add failing E2E coverage that expects the source scope cell to be static, not a textbox.
2. Update the shared log table renderer to honor `readOnlyColumnIds`.
3. Keep existing editable fields unchanged.
4. Capture desktop and mobile screenshots for the study progress table.
5. Update flow rules, audit, PR history, and status docs.
6. Run targeted and full verification before PR.
