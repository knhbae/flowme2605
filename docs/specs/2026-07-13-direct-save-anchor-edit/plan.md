# Plan

1. direct saved map과 personal copy 설정 경로를 분리한다.
2. source version을 보존하는 anchor-only adjustment helper를 만든다.
3. 날짜 기반 personal-state key migration을 pure helper로 만든다.
4. 모바일과 wide My Flow에 동일한 anchor edit form을 연결한다.
5. Calendar 재계산과 항목 override/ICS 보존을 E2E로 검증한다.
6. source-backed 구조 편집 control이 추가되지 않았는지 확인한다.
