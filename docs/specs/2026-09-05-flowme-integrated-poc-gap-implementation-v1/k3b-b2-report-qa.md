# K3-B B2 갱신 보고서 독립 QA

검사일: 2026-09-06 KST. 갭 구현 보고서 (로컬 전용 근거: `../../content-audit/2026-09-05-flowme-integrated-poc-gap-implementation-ko.html`)를 실제 `file:` URL로 읽었다. 제품 PoC 기능을 다시 실행하거나 B1 보고서 QA를 소급 변경한 작업이 아니다.

## 0. 최종 상태

최종 보고서는 `10F7AA47…`이며 현재 B2의 저장 경계 집계와 B1 역사 기록 구분이 보완됐다. RQ-B2-01은 해소됐다. 바뀐 `#boundary`를 다섯 viewport에서 추가 검사해 넘침·캡처 중 상태 변화·오류 0을 확인했다. 링크 대상 92개도 다시 확인했다.

§1~6은 **수정 전 3A0A8A81 보고서의 최초 감사 기록**이다. 최종 HTML의 전체 20개 위치·40개 CTA를 다시 검사한 것으로 표현하지 않는다. 최종 범위와 근거는 §7에 따로 적었다.

## 1. 판정과 범위

보고서 `3A0A8A81…`의 다섯 viewport에서 문서 가로 넘침과 주요 링크 가림을 발견하지 못했다. 375·390 폭의 현재 B2 비교표는 ‘요구·대상 → 수정 전 → 이번 반영·근거’를 모두 세로로 표시한다. 상단·B2 카드·단계표·실행표에서 현재 889B, 다음 B3, 전체 npm 실패 1건, 남은 범위가 구분된다.

남은 보고서 개선 권고는 §5의 경계 증거 문단이다. 현재 B2 수치를 본문에 추가하거나 기존 문단을 B1 역사 기록으로 명시하면 현재 결과를 더 정확히 읽을 수 있다. 이는 제품 기능 결함 판정이 아니다.

- Playwright CLI 0.1.19, Chrome 151.0.7922.138의 새 비영구 격리 세션을 사용했다. 실제 사용자 프로필은 사용하지 않았다.
- 390×844, 375×812, 844×390, 1024×768, 1440×900에서 보고서 1개를 반복 검사했다. 독립 E2E 등록 검사 5개나 제품 시나리오 20개로 세지 않는다.
- 각 화면에서 상단·현재 B2·진행 단계·실행 검사 표를 측정했다. 총 20개 문서 위치 표본이며 가로 넘침은 모두 0이다.
- 상단 링크 3개와 B2 카드 링크 5개를 각 화면에서 스크롤해 노출하고 초점을 옮겼다. 40개 링크 표본의 전체 사각형이 viewport 안에 있고, 내부 360점 모두 실제 링크에 닿았다. 목적지로 이동하는 클릭은 하지 않았다.
- 390 폭에서 실제 Tab 세 번으로 상단 링크 순서와 3px 초점 테두리를 확인했다. 화면 낭독기나 실제 모바일 키보드 검사는 아니다.

브라우저 원시 JSON (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/browser-audit.json`), 집계 JSON (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/summary.json`), 검사 스크립트 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/inspect-report.js`).

## 2. 화면별 결과

| 화면 | 검사 결과 | 대표 근거 |
| --- | --- | --- |
| 390×844 | 4개 위치 넘침 0, CTA 8개 전체 사각형·9점 검사 통과. B2 6행·18셀을 세로로 표시하며 셀 내부 넘침도 없다. 실행표는 기존 내부 가로 스크롤을 유지한다. | 상단 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-top-390x844.png`), B2 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-k3b-structure-390x844.png`), 실행표 최종 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-tests-390x844-loaded.png`) |
| 375×812 | B2 표 폭 317px. 모든 셀의 항목 라벨·본문이 표시되며 줄바꿈이 정상이다. 긴 비교 내용은 문서 아래로 이어지고 겹치지 않는다. | B2 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-k3b-structure-375x812.png`), 단계 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-progress-375x812.png`) |
| 844×390 | B2 표 폭 742px의 세 열을 유지한다. 짧은 높이에서는 세로 스크롤이 필요하지만 핵심 링크를 노출한 뒤 전체 사각형과 9점을 확보했다. | 상단 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-top-844x390.png`), 실행표 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-tests-844x390.png`) |
| 1024×768 | B2 표 폭 922px, 세 열 모두 수평 영역 안에 들어간다. 네 위치 넘침·링크 가림 0이다. | B2 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-k3b-structure-1024x768.png`), 단계 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-progress-1024x768.png`) |
| 1440×900 | B2 표 폭 1,078px. 상단의 현재 결과·남은 npm 실패가 읽히고, B2 전체 비교표와 실제 검사 수의 구분도 읽을 수 있다. | 상단 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-top-1440x900.png`), B2 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-k3b-structure-1440x900.png`) |

375 B2, 1440 상단, 1440 B2, 390 실행표 최종 PNG 4장을 직접 읽었다. 나머지 화면은 DOM 기하·초점·가림 측정 및 캡처 근거이며, 20장 전체를 사람처럼 육안 검토했다고 표현하지 않는다. 과거 검사 표의 내부 가로 스크롤은 문서 전체 가로 넘침과 구분한다. 모바일의 CSS 세로 표가 보조기술에서 어떻게 읽히는지는 미실행이다.

### 캡처 안정성 이력

첫 캡처 20장은 모두 `fullPage:false`다. 그중 19장은 URL·본문 텍스트·스크롤·초점이 전후 동일했지만 390 실행표 한 장은 동일성 검사에 실패했다. 첫 기록에는 전후 전체 값을 남기지 않아 그 차이의 원인을 확정할 수 없다. 해당 PNG를 안정 캡처로 판정하지 않았다.

본문 이미지 2개가 `loading=lazy`임을 확인하고 각각 실제 스크롤과 `decode()` 완료를 기다린 뒤 390 실행표를 별도로 다시 캡처했다. 최종 PNG는 `scrollY=24104` 및 URL·본문·초점이 전후 동일하며 넘침 0이다. 두 이미지 모두 844×390으로 정상 로드됐다. 이는 지연 로딩을 기다린 보조 재캡처 1회이며, 독립 제품 검사 1개를 추가한 것이 아니다. 첫 PNG/JSON은 보존했다.

보조 검사 전체 전후 값 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/cli-loaded.json`), 보조 스크립트 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/check-loaded-images.js`).

## 3. 링크·후보·파일 불변

로컬 링크 검사 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/link-audit.json`)에서 앵커 104개, 고유 href 92개를 수집했다. 이미지 URL 2개는 기존 링크 대상과 중복된다. 고유 로컬 파일 92개가 모두 존재하고 외부 URL과 누락 파일은 0이다. 링크된 문서의 내용이나 과거 시험 전체를 다시 검증한 것은 아니다.

상단과 현재 B2의 ‘직접 조작할’ 링크는 최신 standalone HTML을 가리킨다. 사용자 HTML 두 파일은 모두 1,428,663 bytes, SHA-256 `889B88F811039413B15347359B5490D5D5D66CEFABC3B4B1DA91E4B516733098`이다. 이 감사에서는 제품 페이지를 열지 않고 파일 값만 확인했다.

보고서는 59,863 bytes, SHA-256 `3A0A8A81E6C52BC55F11F4395384ECC2ECABB42EC4155A448077B60FC0E06450`이다. 최초 감사와 지연 이미지 보조 검사에서 보고서·사용자 HTML 2개의 전후 bytes/해시가 모두 같았다.

최초 전후 해시 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/before-after-hashes.json`), 보조 검사 전후 해시 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/loaded-before-after-hashes.json`).

## 4. 오류·저장·도구 경계

보고서 DOM의 script는 0개다. 수집한 콘솔 오류·page error·실패 요청은 0이다. 계측된 reload 이후 `Storage.prototype.getItem/setItem/removeItem/clear/key` 호출은 0이며, 지연 이미지 보조 검사에서도 0이다. 계측용 배열은 reload 때 새로 시작하므로 이를 여러 reload의 누적 API 수라고 표현하지 않는다. 보고서에 저장 기능이 없다는 검사이며 운영 데이터 전체를 전수 검사했다는 뜻도 아니다.

처음 CLI의 `--browser chromium`은 해당 버전의 브라우저 실행 파일이 없어 열리지 않았다. 새 브라우저를 설치하지 않고 기존 Chrome 격리 세션을 사용했다. 이어 기본 CLI 설정이 `file:` 접근을 차단해 실제 보고서를 열지 못했다. 이 두 시도는 도구 제약이며 제품 결함이나 검사 통과로 세지 않는다.

로컬 결과물 검사를 위한 범위 승인을 받은 뒤 고유 임시 CLI 설정 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/cli.config.json`)에 공식 `allowUnrestrictedFileAccess:true` 옵션을 두었다. 전역 설정과 사용자 프로필은 바꾸지 않았고 지정보고서와 그 로컬 href만 읽었다. 설정·실행 스크립트·증거는 보존하며 감사용 격리 브라우저는 종료한다.

## 5. 내용 대조와 남은 권고

- 상단과 현재 B2 카드에는 889B 반영, 다음 B3, 제한 범위 완료가 표시된다. 단계표도 같은 상태다. B1 카드는 ‘이전 FB17 시점 기록’으로 표시돼 과거 결과와 구분된다.
- npm 표는 **2,030개 실행 중 2,029 PASS·1 FAIL**을 명시하고, 후속 201+19 통과를 별도로 적는다. build 성공을 npm 전체 통과나 배포로 바꾸어 표현하지 않는다.
- B2 모델 750/750·새 UI 12/12·기존 UI 54/54·생성 151/151·선정 gate/시간 5/5·file 1/1·host 8/8은 보고서가 인용한 별도 실행 근거다. 이 보고서 감사에서 그 검사들을 새로 실행하거나 합산하지 않았다.
- 원본 전체 충족률, 출시 완료, 실제 사용자 관찰 완료라는 주장은 없다. 남은 B3·K3-C·K4와 실기/IME/보조기술 미실행도 표시한다.

**RQ-B2-01 — 현재 경계 문단의 시점 구분 권고.** `#boundary`는 B1 mode/닫기 수치 뒤에 ‘C3는 전체 API 합계를 수집하지 않았다’고 적는다. B1 역사 결과로는 맞지만 현재 B2의 [54개 회귀 QA](k3b-structure-existing-ui-regression-qa.md)는 실제 경계 첨부 98개·제품 API 395회를 수집했다. 현재 B2 문단을 추가하거나 기존 문단 전체를 B1 역사로 표시하는 편이 정확하다. 현재 경계 표가 잘못된 파일을 가리키거나 운영 데이터 불변을 과장했다는 결론은 아니다. 보고서 편집 담당에게 전달했으며 이 독립 QA에서 보고서를 수정하지 않았다.

현재 비교표는 모바일에서도 충족 전후와 근거를 모두 읽을 수 있다. 다만 전체 보고서에는 긴 역사 기록이 이어지므로 과거 시험 표의 상세 설명은 가로 스크롤로 읽어야 한다. 이를 현재 B2 비교표의 숨은 셀이나 페이지 깨짐으로 혼동하지 않는다.

## 6. 변경·미실행

새 파일은 이 QA와 고유 output 폴더의 감사 스크립트·설정·JSON·PNG다. 제품 소스, 기존 검사, 보고서 생성기, 보고서/사용자 HTML, B1 QA는 수정하지 않았다.

실제 Android Chrome·iOS Safari·IME·보조기술: **NOT_RUN**. 관찰 사용자: **0명**. commit·push·PR·Preview·Production: **미실행**. 이번 작업은 보고서 표시와 로컬 파일 연결 QA이며 제품 요구사항 전체의 완료 판정이 아니다.

## 7. 10F7AA47 최종 경계 문단 보완 재검사

보고서 작성 담당자가 `#boundary`의 한 문단에 현재 B2 신규 12개 시나리오의 23 context·66 API, 기존 54개 회귀의 경계 기록 98개·395 API, 실제 사용자 HTML file 검사의 8 API를 구분해 적었다. C3의 명시 reset·삭제·구버전 복구가 허용된 PoC old key를 바꿀 수 있다는 예외와, B1 미집계 기록의 시점도 명시했다. 따라서 RQ-B2-01은 해소됐다. 제품·CSS는 이 수정의 대상이 아니다.

최종 보고서는 **60,059 bytes**, SHA-256 `10F7AA477383D066A89DC67E9824A2EB376BC90FE13B2A9EBF175870D8F1EF1A`이다. 새 격리 세션에서 실제 같은 `file:` URL을 열고 **바뀐 경계 문단 5개 viewport 표본**을 검사했다. 최초 전체 20개 위치 검사와 합쳐 최종 HTML의 25개 위치 통과로 세지 않는다.

- 390×844·375×812·844×390·1024×768·1440×900 모두 문서 넘침 0, 문단 좌우 잘림·내부 넘침 0이다.
- `fullPage:false` PNG 5개 모두 캡처 전후 URL·본문·초점·스크롤이 같다. 390과 1440 PNG 2개를 직접 읽어 현재/과거 집계와 PoC 쓰기 예외가 정상 줄바꿈으로 표시됨을 확인했다.
- 상단 제목과 lead는 최초 문자열과 동일하다. B2 카드도 레이아웃 공백을 정규화한 본문이 동일하다. 원시 `innerText`는 모바일 세로 셀의 줄바꿈과 데스크톱 셀의 탭 때문에 다르며, 이를 내용 변경이나 동일 bytes로 처리하지 않았다.
- 링크 104개·고유 파일 대상 92개, 누락·외부 URL 0을 다시 확인했다. 최종 관측 script·storage 호출·console error·page error·실패 요청은 모두 0이다.
- 보고서와 사용자 HTML 두 파일의 해당 검사 전후 bytes/해시가 같다. 사용자 HTML은 계속 889B / 1,428,663 bytes이며 제품을 브라우저로 실행하지 않았다.

최종 원시 검사 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/cli-boundary.json`), 최종 집계·파일 존재 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/boundary-summary.json`), 최종 전후 해시 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/boundary-before-after-hashes.json`), 보완 스크립트 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/check-boundary-update.js`).

직접 읽은 최종 화면: 390×844 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-updated-boundary-390x844.png`), 1440×900 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-updated-boundary-1440x900.png`). 나머지: 375×812 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-updated-boundary-375x812.png`), 844×390 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-updated-boundary-844x390.png`), 1024×768 (로컬 전용 근거: `../../../output/playwright/k3b-b2-report-20260906-01/report-updated-boundary-1024x768.png`).

등록 자동 시험을 새로 실행하지 않았다. 문서 검사 `npm run docs:check`는 별도로 실행했다. 감사용 브라우저 두 세션은 각각 종료했고 임시 설정과 과거 증거는 보존한다. 이 QA 작성자는 제품·시험·기존 문서·보고서를 직접 수정하지 않았다.
