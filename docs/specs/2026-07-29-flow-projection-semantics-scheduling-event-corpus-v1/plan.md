# Plan

## Phase 1 — Freeze and audit

- Freeze the 42-fixture canonical corpus and record hashes.
- Compare taxonomy contracts, corpus builders, runtime types, and export code.
- Separate canonical entity, projection, export format, destination capability,
  iCalendar component, and schedule ownership.

## Phase 2 — Contract design

- Finalize Checklist/Todo decision rules and distinct schemas.
- Define recommendation, availability, fidelity, generation, and loss axes.
- Define temporal intent, pacing, grouping, event series/edition/occurrence, and
  iCalendar mapping.
- Score three architecture alternatives with the same evidence.

## Phase 3 — Corpus and generation

- Add at least six Checklist/Todo boundary fixtures.
- Add at least twelve event-native fixtures from directly inspected URLs.
- Generate all five projections for every frozen fixture and every new fixture
  when eligible.
- Record empty/prohibited outputs rather than manufacturing records.

## Phase 4 — Validation

- Validate schemas, controlled enums, counts, provenance, pacing determinism,
  no invented source schedules, no due-to-VEVENT coercion, no false yearly
  recurrence, bundle child IDs, and fallback coverage.
- Compare an independent classifier with the rule-based adjudication.

## Phase 5 — Handoff

- Create a five-minute Korean decision summary.
- Create a roomy PPT-like HTML report with one question or case per section.
- Verify the report at 1440×900 and 390×844.
- Record external Calendar/VTODO and observed-user validation as `NOT_RUN`.
