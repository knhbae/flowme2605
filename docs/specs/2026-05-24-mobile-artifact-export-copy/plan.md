# Mobile Artifact Export Copy Plan

1. Update the mobile E2E expectation first so the old generic labels fail.
2. Rename the sticky action and bottom sheet copy to artifact-specific language.
3. Keep the mobile bar compact: no separate copy/excel buttons in the sticky bar.
4. Document the UX rationale and natural artifact simulation.
5. Capture the mobile bottom sheet screenshot.
6. Run build, tests, docs check, diff check, E2E, PR, and merge if checks pass.

