# Plan

1. Capture the repository, worktree, branch, PR, deployment, and dirty-file
   baseline.
2. Attach detached work and isolate the dirty former-main state on named
   branches without changing file contents.
3. Open and verify one integration PR for the deployed P35 Round 2 source.
4. Decide the disposition of superseded P35 worktrees and the design-handoff
   draft PR.
5. Split operations, research, and project-control changes into reviewable
   packages while leaving Text Authoring and content review isolated.
6. Reconcile the canonical status and backlog after the production source is
   durable on `main`.
7. Remove only proven temporary outputs and safely closed worktrees.
8. Run the appropriate verification baseline and record the next bounded
   refactoring entry point.

