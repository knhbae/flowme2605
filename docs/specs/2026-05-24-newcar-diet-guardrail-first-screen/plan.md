# Plan

## Implementation

1. Add a RED E2E guardrail test for new-car and diet first screens.
2. Add a warning card inside decision-table workbenches when a Flow has a warning.
3. Specialize the new-car warning card title as `인수 전 보류 기준`.
4. Add diet-specific spreadsheet columns with `중단/상담 조건`.
5. Add a sheet heading and description so diet reads as observation, not coaching.
6. Capture desktop and mobile screenshots.
7. Document natural artifacts, UX gaps, risk boundaries, and verification.

## Review Checkpoints

- The new-car route must not imply signing or accepting is safe.
- The diet route must not imply target weight, diagnosis, or prescription.
- The first-screen additions must not introduce a larger native record system.
