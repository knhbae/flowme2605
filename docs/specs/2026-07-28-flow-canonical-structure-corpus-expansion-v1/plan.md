# Plan

1. Freeze the 8-fixture Qualified v2 baseline and hash every input artifact.
2. Merge candidate lineage from discovery, deep-set, P0, value-qualified,
   creator-portfolio, Qualified v2, and vertical research.
3. Deduplicate by normalized canonical URL and stable source job.
4. Predeclare the structural coverage matrix.
5. Convert in deterministic batches:
   - Batch 0: frozen baseline,
   - Batch 1: complete deep-set packets,
   - Batch 2: non-duplicate value-qualified gold packets,
   - Batch 3: non-duplicate frozen P0 source-row packets.
6. Keep incomplete deep-set cases as a small boundary appendix, not complete
   fixtures.
7. Generate the canonical corpus, SourceRow mapping, projection contract,
   representative DTOs, decision register, runtime crosswalk, and saturation log.
8. Validate schema and semantic invariants.
9. Build a 30–40 screen Korean explanation deck plus a lazy/detail corpus explorer.
10. Verify desktop/mobile rendering, filters, overflow, images, console, docs, and
    scoped worktree ownership.

## Working-file ownership

Owned by this goal:

- `docs/specs/2026-07-28-flow-canonical-structure-corpus-expansion-v1/`
- `docs/content-audit/2026-07-28-flow-canonical-structure-corpus-expansion-review-ko.html`

Every pre-existing dirty or untracked path is unowned and must be preserved.

## Non-goals

- no public promotion adjudication,
- no low-cost/high-capability model benchmark,
- no UX or app implementation,
- no DB/API/crawler/provider work,
- no external Calendar account round trip,
- no commit, push, PR, merge, or deployment.

