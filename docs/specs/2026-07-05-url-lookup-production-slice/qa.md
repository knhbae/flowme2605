# URL Lookup Production Slice QA

## Required Checks

| Check | Result | Evidence |
| --- | --- | --- |
| `npx tsx --test lib/flow/storage.test.ts` | Pass | 2026-07-05: RED failed because `normalizeSavedFlowMapSnapshot` dropped `personalCopy`; after adding personal-copy normalization, 8/8 tests passed. |
| `npx tsx --test lib/flow/source-backed-my-flow.test.ts` | Pass | 2026-07-07: 51 source-backed map tests pass in the combined 56/56 source/report targeted run. Duplicate coverage proves `funmom-study-routine-map`, `opic-plan-map`, `curated-baby-food-meal-log`, `reading-routine-map`, `new-car-map`, `homefit-map`, `moving-map`, `vaccination-map`, and `wedding-map` remain directly publishable but leave URL lookup direct routing with `directRouteEnabled=false`; sourceTrace coverage now proves `aircon-filter-cleaning`, `curated-reading-routine-log`, `moving-d30`, `curated-ajd-moving-d30`, `curated-new-car-purchase-guide`, `middle-school-math-1`, `curated-opic-mock-course`, `curated-wedding-checklist-family`, `curated-allblanc-workout-park`, `curated-child-vaccination-schedule`, `baby-health-schedule`, `curated-funmom-learning-park`, `postal-address-transfer`, and `year-end-tax-submit` are no longer blocked by missing sourceTrace. |
| `npx tsx --test lib\flow\source-backed-manual-registration-report.test.ts` | Pass | 2026-07-07: 5 report tests pass in the combined 56/56 source/report targeted run. Duplicate canonical URL coverage proves the report has 0 duplicate canonical URL map memberships and 0 groups, and sourceTrace queue coverage proves every lookupable source-backed map moved to `QA 통과`, missing sourceTrace dropped to 0 maps / 0 Steps, and the sourceTrace remediation queue is empty. |
| `npx tsx --test lib\flow\source-backed-my-flow.test.ts lib\flow\source-backed-manual-registration-report.test.ts` | Pass | 2026-07-07: 56/56 targeted tests passed after the thirteenth sourceTrace queue repair. The report duplicate count remains 0 lookupable map memberships across 0 groups, `year-end-tax-submit` is now QA-pass ready, and the sourceTrace remediation queue is empty. |
| `npx tsx --test lib\flow\url-first-lookup.test.ts lib\flow\source-backed-my-flow.test.ts lib\flow\source-backed-manual-registration-report.test.ts` | Pass | 2026-07-07: 75/75 targeted tests passed across URL lookup, source-backed registration readiness, and the manual registration QA report after the year-end-tax sourceTrace repair. The NTS source URL still resolves to `/flow-maps/year-end-tax-submit`, duplicate canonical URL groups remain at 0, and ePost postal, Funmom, EasyLaw baby-health, KHMS child vaccination, Allblanc, Naver wedding, Mansour OPIC, Mathbang, Getcha, AJD moving, Naver reading, and baby-food duplicate URLs still resolve to their selected representatives. |
| `npx tsx --test lib/flow/my-flow-step-export.test.ts` | Pass | 2026-07-05: 4/4 tests passed. New coverage proves current Step export can produce memo/Markdown text, checklist text, dated `.ics`, and one sheet-row TSV from edited/personal-copy fields. |
| `npx tsx --test lib/flow/url-first-supply-queue.test.ts` | Pass | 2026-07-05: RED first failed because `url-first-supply-queue.ts` did not exist. After implementation, 7/7 tests passed for `miss_request`, `needs_review_request`, canonical duplicate dedupe, malformed queue normalization, candidate edit, candidate delete, and resolved-hit availability. |
| `npx tsx --test lib/flow/url-first-lookup.test.ts` | Pass | 2026-07-05: RED first failed with `miss !== hit`; after source-backed registry expansion, 5/5 tests passed. Startable follow-up RED failed because `buildUrlFirstStartPackage` was missing; after implementation, 8/8 tests passed. Lightweight customization RED failed because custom title and empty Step selection were ignored; after implementation, 10/10 tests passed. |
| `npx tsx --test lib/flow/url-first-lookup.test.ts lib/flow/url-first-supply-queue.test.ts` | Pass | 2026-07-05: 22/22 URL lookup and supply queue domain tests passed together after adding manual registration loop coverage. The RED run failed with `miss !== hit` and `missing !== executable` for the Samsung service aircon filter source URL; after registering direct-route-enabled, non-rejected source-backed maps in URL lookup, the same tests passed and built a start package for `aircon-filter-cleaning`. |
| `npx tsx --test lib\flow\source-backed-my-flow.test.ts lib\flow\url-first-lookup.test.ts` | Pass | 2026-07-06: 40/40 targeted tests passed. This locks the manual registration checklist/readiness helper, requires source URL + `directRouteEnabled` + non-`reject` for lookupable source-backed maps, and proves the rejected KISA source-backed URL stays a miss. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "flow finding URL lookup"` | Pass | 2026-07-05: RED first failed because `flow-url-lookup-entry` was missing; after `/flows` UI implementation, hit/needs_review/miss state test passed. Startable follow-up RED failed because the `시작일` control was missing; after implementation and a hydration wait guard, hit/needs_review/miss plus start/save/Markdown export tests passed. Lightweight customization coverage now passes direct start, customized personal copy, and blocked review states together: 3/3 tests. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "starts a hit"` | Pass | 2026-07-05: Mathbang URL hit accepted `2026-07-15`, selected Markdown, downloaded `middle-school-math-1-flow.md`, saved My Flow records and `flow:map:saved:middle-school-math-1`, confirmed direct non-personal starts do not expose personal-copy settings, then blocked start controls for needs_review. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "lightweight customized personal copy"` | Pass | 2026-07-05: Mathbang URL hit switched to `조금 고쳐 시작`, saved title `시험 전 소인수분해만`, kept only the first Step, downloaded Markdown with the custom title and without the excluded Step, saved customized map snapshot/persistence record, and wrote excluded Step item state as skipped. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "my flow shows update review notice\|my flow mobile saved map edit"` | Pass | 2026-07-05: source-backed update review and saved-map mobile revisit tests passed after personal-copy persistence changes. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "personal copy settings can readjust"` | Pass | 2026-07-05: RED first failed because My Flow had no personal-copy settings entry. After implementation, the test saves a lightweight custom copy, opens `/my` settings, changes the saved title and start date, excludes the included Step, re-includes an excluded Step, verifies active/excluded sections, regenerates Markdown from Step detail, and confirms source-backed update apply does not overwrite the readjusted state. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "personal copy step detail exports current copy"` | Pass | 2026-07-05: RED first failed because the Step detail export area had no personal-copy basis note or checklist/TSV copy actions. After implementation, the test saves a URL-customized moving personal copy, readjusts title/start date, verifies excluded Steps stay outside active flow, copies memo/checklist/TSV, downloads dated `.ics`, and confirms the adjusted date appears in `/calendar`. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "saves production candidates"` | Pass | 2026-07-05: RED first failed because the miss result had no `아직 실행 가능한 Flow 아님` candidate request area. After implementation, the test saves an unknown URL as `miss_request`, confirms a duplicate canonical URL shows the existing candidate without adding another record, then saves a needs-review URL as `needs_review_request`. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "production candidates can be revisited"` | Pass | 2026-07-05: RED failed because the requested-candidate list only displayed static saved rows. After implementation, the test seeds local candidates, verifies `제작 대기` / `원문 확인 대기` / `이제 실행 가능`, opens the original URL link, edits title/memo, re-runs canonical lookup, deletes a candidate, and moves a resolved hit candidate into the normal URL hit/start panel. |
| `npx tsx --test lib\flow\url-first-supply-queue.test.ts` | Pass | 2026-07-05: Candidate-to-production handoff RED failed because `recordUrlFirstSupplyCandidateLookup` and `buildUrlFirstSupplyCandidateProductionMarkdown` were missing. After implementation, 10/10 supply queue tests passed, including `lastLookup` persistence and AI-free production handoff Markdown with the manual conversion checklist. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "production handoff markdown"` | Pass | 2026-07-05: RED failed because requested-candidate cards had no `제작용 정보 보기` button. After adding the collapsed production handoff panel and rebuilding `.next`, the test passed: pending candidates show canonical/original URL, last lookup, checklist, and clipboard Markdown; resolved-hit candidates keep `Flow 결과로 이동` as the priority path. |
| `npx playwright test -g "manual registered production candidate" tests/e2e/flow-mvp.spec.ts` | Pass | 2026-07-05: RED failed because a local candidate for `https://www.samsungsvc.co.kr/solution/28524` stayed in production wait instead of showing `이제 실행 가능`. After the source-backed lookup registry change and production rebuild, the test passed: the candidate moved to the hit result, `/flow-maps/aircon-filter-cleaning` was available, starting with `2026-07-06` saved `source-backed-aircon-filter-cleaning` into My Flow. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "production handoff markdown\|production candidates can be revisited\|flow finding URL lookup"` | Pass | 2026-07-05: 6/6 URL lookup, production candidate save/revisit/edit/delete/resolved-hit, production handoff Markdown, direct start, and customized personal-copy start tests passed together after a clean build. The first clean-build grouped run exposed a cold-start fallback timeout on early `/flows` tests; an immediate rerun passed once the server and client chunks were warm. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "flow finding URL lookup\|personal copy settings can readjust\|personal copy step detail exports current copy"` | Pass | 2026-07-05: 6/6 URL lookup, supply candidate, direct start, lightweight custom personal copy, personal-copy readjustment, and personal-copy Step export tests passed together. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "flow finding URL lookup\|personal copy settings can readjust\|my flow shows update review notice\|my flow mobile saved map edit"` | Pass | 2026-07-05: 6/6 targeted URL lookup, personal-copy readjustment, update review, and mobile saved-map tests passed together. |
| `npx playwright test tests/e2e/flow-mvp.spec.ts -g "flow list exposes the seed"` | Pass | 2026-07-05: first run caught catalog card top at 519px after adding URL entry; layout was compressed to keep the catalog first card under the existing 480px guard, then the test passed. |
| `npm.cmd run docs:check` | Pass | 2026-07-07: passed with 14 required files and 1522 local links after adding the thirteenth sourceTrace queue repair, regenerated report evidence, and updated QA links. |
| `npx tsc --project tsconfig.next.json --noEmit --pretty false` | Pass | 2026-07-07: build-scoped TypeScript check passed after the year-end-tax sourceTrace repair. The production type gate still uses `./tsconfig.next.json`, so app/runtime code validates separately while `npm test` remains the executable test gate for test files. |
| `npm test` | Pass | 2026-07-07: passed 361 node tests after the thirteenth sourceTrace queue repair, including duplicate canonical URL grouping staying at 0 groups, manual source-backed registration checklist/readiness coverage, sourceTrace remediation queue closure, `curated-reading-routine-log`, `moving-d30`, `curated-ajd-moving-d30`, `curated-new-car-purchase-guide`, `middle-school-math-1`, `curated-opic-mock-course`, `curated-wedding-checklist-family`, `curated-allblanc-workout-park`, `curated-child-vaccination-schedule`, `baby-health-schedule`, `curated-funmom-learning-park`, `postal-address-transfer`, and `year-end-tax-submit` QA-pass coverage, rejected URL lookup exclusion, URL lookup customization, URL-first supply queue candidate edit/delete/resolved-hit/production-handoff/manual-registration coverage, source-backed personal-copy readjustment, and Step export checklist/TSV coverage. |
| `.\node_modules\.bin\next build` | Pass | 2026-07-06: clean direct Next production build passed using `./tsconfig.next.json`, compiled successfully, generated 16/16 static pages, and collected build traces with normal route bundle sizes. |
| `cmd /c ".\node_modules\.bin\next.cmd build"` | Pass | 2026-07-06: clean Windows cmd shim build passed with the same route bundle sizes as direct Next, proving the `next.cmd` shim itself was not the root cause of the earlier `.next` ENOENT failures. |
| `npm.cmd run build` | Pass | 2026-07-07: passed after the thirteenth sourceTrace queue repair through the committed direct `next build` script. The build compiled successfully, used `./tsconfig.next.json`, generated 16/16 static pages, finalized page optimization, and collected build traces. Earlier wrapper and `webpackBuildWorker=false` experiments remain outside the committed build gate because direct build is stable once stale `npm run build` / `next build` worker processes are cleared. The standard verification command remains `npm.cmd run build`. |
| `npx tsx scripts\content-audit\build-source-backed-manual-registration-qa-report.ts` | Pass | 2026-07-07: generated [manual registration QA report](../../content-audit/2026-07-06-source-backed-manual-registration-qa-ko.html). Current source-backed state after the first sample repair, nine duplicate repairs, and thirteen sourceTrace queue repairs: 26 total maps, 15 lookup-eligible, 15 QA-pass, 0 registration holds, 11 lookup-blocked. QA-pass maps are now `aircon-filter-cleaning`, `baby-food-map`, `curated-reading-routine-log`, `moving-d30`, `curated-ajd-moving-d30`, `curated-new-car-purchase-guide`, `middle-school-math-1`, `curated-opic-mock-course`, `curated-wedding-checklist-family`, `curated-allblanc-workout-park`, `curated-child-vaccination-schedule`, `baby-health-schedule`, `curated-funmom-learning-park`, `postal-address-transfer`, and `year-end-tax-submit`; duplicate canonical URL membership remains 0 maps across 0 groups. Remaining blockers are now 0 maps with missing sourceTrace issues / 0 Steps, and the generated report exposes an empty sourceTrace remediation queue. |
| 390px manual registration QA report browser check | Pass | 2026-07-06 pre-ninth repair: opened `docs/content-audit/2026-07-06-source-backed-manual-registration-qa-ko.html` as a local file, verified one `qa_pass` row for `aircon-filter-cleaning`, verified the then-current duplicate canonical URL group table, verified several lookup-blocked rows, and confirmed overflow 0. After the thirteenth repair the current duplicate 0 state, year-end-tax QA-pass state, and empty sourceTrace queue are covered by the generated report and targeted tests above, not by these older screenshots. |
| 390px `/flows` browser QA | Pass | 2026-07-05: `http://127.0.0.1:3104/flows`, Mathbang URL hit result, custom start panel visible, one checked Step after exclusions, overflow 0, console 0, screenshot `output/playwright/url-lookup-custom-start-mobile-260705.png`. |
| 390px `/flows` supply candidate browser QA | Pass | 2026-07-05: `http://127.0.0.1:3104/flows`, unknown URL saved as a local production candidate, stored count 1, candidate list visible above catalog, overflow 0, app console errors 0, page errors 0, screenshot `output/playwright/url-first-supply-candidate-mobile-260705.png`. One resource-load 404 console message was observed separately and did not produce page errors. |
| 390px `/flows` candidate management browser QA | Pass | 2026-07-05: `http://127.0.0.1:3104/flows`, seeded three local requested candidates, verified `제작 대기`, `원문 확인 대기`, `이제 실행 가능`, original URL/requery/resolved-hit actions, title/memo edit result, article count 3, overflow 0, page errors 0, screenshot `output/playwright/url-first-candidate-management-mobile-260705.png`. One resource-load 404 console message was observed separately and did not produce page errors. |
| 390px `/flows` candidate-to-production handoff browser QA | Pass | 2026-07-05: `http://127.0.0.1:3104/flows`, seeded pending and resolved local requested candidates, opened `제작용 정보`, copied production Markdown, verified canonical URL and no-AI/no-crawl note in clipboard, verified resolved-hit card keeps `Flow 결과로 이동` visible, overflow 0, console errors 0, page errors 0, screenshot `output/playwright/url-first-candidate-production-handoff-mobile-260705.png`. |
| 390px `/flows` manual registration loop browser QA | Pass | 2026-07-05: Playwright webServer production run, seeded a local candidate for `https://www.samsungsvc.co.kr/solution/28524`, verified a visible Flow handoff action, opened the normal hit/start panel for `/flow-maps/aircon-filter-cleaning`, overflow 0, page errors 0. Console showed two known resource-load 404 messages and no app page error. Screenshots: `output/playwright/url-first-manual-registration-candidate-mobile-260705.png`, `output/playwright/url-first-manual-registration-start-mobile-260705.png`. |
| 390px `/my` personal-copy settings browser QA | Pass | 2026-07-05: `http://127.0.0.1:3104/flows` -> `/my?savedMap=middle-school-math-1`, created a URL-customized personal copy, opened `설정 조정`, changed title/date draft, toggled included/excluded Step choices, overflow 0, console errors 0, screenshot `output/playwright/my-flow-personal-copy-settings-mobile-260705.png`. |
| 390px `/my` personal-copy Step export browser QA | Pass | 2026-07-05: `http://127.0.0.1:3104/flows` -> `/my?savedMap=moving-d30`, created a URL-customized personal copy, opened the first Step detail, verified `원문·내 도구` is collapsed by default, expanded memo/checklist/TSV/ICS actions, overflow 0, app console errors 0, page errors 0. Screenshots: `output/playwright/my-flow-personal-copy-step-export-mobile-collapsed-260705.png`, `output/playwright/my-flow-personal-copy-step-export-mobile-expanded-260705.png`. Resource-load 404 console messages were observed separately and did not produce page errors. |

## Review Notes

- Product constraint review: URL lookup belongs in `/flows`, not Home, and keeps AI generation disabled in P0.
- Source/risk review: hit results may show source/trust context, but user-facing primary copy must avoid internal labels and fake validation.
- Build stabilization review: `next.config.ts` keeps `typescript.tsconfigPath=./tsconfig.next.json`; `tsconfig.next.json` remains the build-scoped type config. The Windows `next.cmd` shim and direct `next build` have passed from a clean `.next` directory with the default webpack build worker enabled, so the committed `npm.cmd run build` gate stays on direct `next build`. In the ninth sourceTrace repair run, build failures reproduced while stale repo-local `npm run build` / `next build` worker processes were still alive and holding `.next`; after stopping those processes and removing only the repo-local `.next`, `npm.cmd run build` passed. `experimental.webpackBuildWorker=false` reproduced a silent compile-stage failure and partial `.next` output in this workspace, so that setting is not part of the committed baseline. The cleanup/retry wrapper was evaluated but is not part of the committed standard gate; if stale `.next` artifacts recur, stop stale build processes, remove only the repo-local `.next`, and rerun the direct gate before proposing a wrapper change.
- Browser or screenshot review: Targeted `/flows` Playwright checks passed for URL lookup, supply candidate requests, candidate revisit/edit/delete/resolved-hit handoff, candidate-to-production handoff, manual-registration resolved-hit start, direct start/save/export, lightweight customized personal copy, catalog first-screen rhythm, and `/my` personal-copy readjustment. Mobile screenshot QA passed with the custom start panel, local supply candidate list, candidate management cards, manual-registration resolved candidate, My Flow personal-copy settings, and personal-copy Step export visible; the Step export panel is collapsed by default on mobile, overflow is 0, and page errors are 0.
- Residual risk: This slice uses existing source-backed data, quality decisions, and static lookup behavior; it is not live web extraction, AI generation, full editing, direct calendar integration, account-backed production queues, observed-user validation, or a fix for non-`.next` filesystem/antivirus/process-lock failures. The sourceTrace queue is now empty under the current manual registration QA rules, but this is still operator-readiness evidence, not observed-user validation or a tax/legal correctness claim. If a build or external tool leaves stale build processes alive, stop those stale processes, clean repo-local `.next`, and rerun the standard direct build gate.
