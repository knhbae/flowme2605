# 기존 구현과 와이어프레임 연결

2026-09-07 읽기 전용 대조. 기존 개발3는 `D:/flowme2605/flow-personal-workspace-v4-1-poc-20260901`, HEAD `6e4b44fe` 이후 미커밋 작업을 포함한다. 아래는 코드를 읽은 결과이며 이번 제품 테스트 결과가 아니다.

## 유지 / 연결 조정 / 신규 검토

| 사용자 흐름 | 현재 개발3의 근거 | 구분과 후속 작업 |
| --- | --- | --- |
| 자유 TXT 입력·보존 | `PersonalWorkspacePocAuthoringSurface.tsx:1130-1174`, `personal-workspace-poc-storage.ts:27-46,92-152`에서 rawText 저장·복구. 구조 없을 때 원문 유지(`AuthoringSurface.tsx:3506-3517`) | 보존 기반 유지. 새 저장기를 중복 구현하지 않도록 후속 검토 |
| 개인 문서로 쌓고 다시 열기 | 여러 원문은 `PersonalWorkspacePocCreatorDraftLibrary.tsx:170-246` 및 `creator-drafts.ts:8-45`로 보관. 현재는 제작자 초안이고 개인공간·완료·날짜와 분리(`AuthoringSurface.tsx:3593-3622`) | 개인 문서 진입·보관 UX와 소유 계약은 신규/연결 검토. 본 시뮬레이터는 문서 1개만 다룸 |
| 필요한 줄만 할 일 연결 | 현재 `- [ ]` 행이 Item이고 `내 Flow에 저장`이 개인 상태 commit 조건(`AuthoringSurface.tsx:2810-2922,3214,3609-3619`) | 임의 문장 선택→개인 실행 항목은 미구현. 이번 안은 명시 선택·원문 보존을 제안 |
| 오늘 완료 후 문서 복귀 | `PersonalWorkspacePocSurface.tsx:4595-4637,5396,5846-5867,6316-6329`에 기간/완료/다시 열기. 초기 section은 folder(`:900`) | 기간·완료 동작 유지. 문서의 연결 항목과 원문 복귀는 연결 필요. 마지막 화면 복원은 미확정 |
| 공개 Flow→개인 사본 | `AuthoringSurface.tsx:2935-2978` 검색은 기기에 저장된 사본. `k3c-c1-entry-preview-design.md:71`도 공개 catalog가 아니라고 명시 | 이 PoC에서 공개 발견→사본 생성은 미연결. 기존 제품 공개 상세와의 연결을 별도 개발 단위로 검토 |
| 개인 사본 찾기·수정 | `PersonalWorkspacePocEntryPreview.tsx:79-125`, `AuthoringSurface.tsx:2314-2358,3354-3379`, `Surface.tsx:4775-4784` | 기존 사본 검색·미리보기·편집 기반 유지. 검색 범위와 편집 대상 표시 조정 |
| 개인 날짜→내부 실행 | `Surface.tsx:5396,5846-5867`, `personal-workspace-poc-contract.ts:562-583` | 개별 날짜·완료 유지. 기준일 일괄 재계산·포함/제외·원문 정렬은 K4 설계 단계이며 이 실험으로 구현 완료 처리하지 않음 |
| 외부 도구 전달 | `PersonalWorkspacePocResultPresenter.tsx:278-379`, `personal-workspace-poc-result-projection.ts:323-342,1010-1049`에 TXT/CSV 출력 | 기존 출력 기반 유지. 이 와이어프레임은 TXT만 시뮬레이션. 실제 외부 앱 가져오기·재사용·왕복은 미검증 |

파일명은 개발3의 `components/flow/personal-workspace-poc/` 및 `lib/flow/` 아래를 가리킨다. 위 표는 2026-09-07 대조 당시의 기록이며 이후 개발 상태를 자동 반영하지 않는다. 당시 `progress.md`의 로컬 절대 경로와 해석 범위는 [근거 보존 기록](./evidence-context.md)에 남겼다. 다른 워크트리를 열 수 없어도 이 표를 읽을 수 있지만, 후속 구현 전에는 실제 개발 체크아웃을 다시 확인해야 한다.

## 다음 개발 후보 — 승인 대기

1. **개인 문서 저장·재진입**: 문서가 Flow 전 단계의 임시 입력으로만 보이지 않도록 한다. 빈 문서·다수 문서·폴더·최근 문서·저장 실패·복원까지 작은 완결 범위로 정의한다. 자동 저장 여부는 이번 명시 저장 실험만으로 결정하지 않는다.
2. **문서의 선택 부분과 실행 항목 연결**: 생성 전에 선택·날짜를 확인한다. 중복 줄, 원문 삽입/삭제/이동, 연결 끊기, 제목·완료·날짜 반영 범위를 먼저 계약으로 정한다. 오늘 화면은 같은 ID를 투영한다.
3. **공개 상세에서 개인 사용/외부 전달 연결**: 공개본 ID·버전·권리·복사 실패·중복 가져오기·원본 업데이트 보존을 설계한다. 외부 전달을 위해 개인 저장을 강제하지 않는다.

위 순서는 UX 관점의 추천이다. 기존 K4-A1 개발 기록을 취소하거나 다음 목표를 자동 교체하지 않는다. 정리 세션의 최신 보존·진행 목록과 사용자 검토 후 개발 범위를 승인한다.

## 의도적으로 남긴 범위

- 폴더 이동·드래그·주간/월간 전체 화면: 기존 v4.1 기반을 유지하며 이 HTML에서 다시 구현하지 않음.
- 문서 1개, 공개 예시 1개, 사본 1개만 시뮬레이션. 실제 계정·클라우드·TXT 파일 원본 저장·다중 문서 저장 계약 없음.
- wiki: 출처·버전·관련 지식의 연결 위치만 표시. 분류 체계·범용 공개 편집 권한 미확정.
- 커뮤니티: 질문/버전 지정 제안 초안의 위치만 표시. 실제 전송·수신·검토·결과 알림 미구현.
- 파생 Flow 발행: 개인 메모·완료·날짜를 제외한 후보 미리보기만 표시. 권리 확인·승인·발행·철회는 후속 설계.

큰 방향은 [9월 6일 방향의 보존 요약](./evidence-context.md)을 따른다. 이번 화면에서 제안한 문장 선택 방식과 공개 첫 버튼 순서는 확정 정책이 아니다. 최신 정책은 개발 착수 시 저장소 정본과 다시 대조한다.
